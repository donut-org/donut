# CLI wrapper — implementační plán

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Spustit workflow z příkazové řádky, vypsat seznam workflow a nápovědu k jednomu z nich.

**Architecture:** Tři jednotky — `Cli\Arguments` je čistý rozklad `$argv`, `Cli\Application` složí repozitáře a přeloží výjimky na návratové kódy, `bin/donut` je dvouřádkový. Předchází tomu přejmenování klíčů na camelCase a dvě změny v hotovém runneru, bez kterých CLI nedrží pohromadě.

**Tech Stack:** PHP 8.1+, nette/utils ^4.1.4, nette/tester ^2.6, PHPStan level max.

## Global Constraints

- PHP `>= 8.1`. Typed properties, `readonly`, constructor promotion.
- `nette/utils` `^4.1.4`. Procesy jen přes `Process::runExecutable()`, **nikdy** `runCommand()`.
- Žádná databáze, žádné contributte balíčky, žádné `nette/di`. **Žádný hotový console framework** — vlastní wrapper nad `$argv`, jak říká zadání.
- Namespace `Donut\`, PSR-4 na `src/`.
- PHPStan level max nad `src` i `tests` musí projít.
- Testy jsou `.phpt` pro nette/tester, spouštěné přes `make test`.
- Jazyk kódu a identifikátorů je angličtina. Chybové hlášky pro uživatele česky.
- Zástupné vzory nette/testeru: `%a%` bez odřádkování, `%A%` s ním, obojí vyžaduje **aspoň jeden znak**; na začátku a konci hlášky patří `%a?%` / `%A?%`.
- **Referenční pravda je `docs/format-specifikace.md` v0.3 a `docs/superpowers/specs/2026-08-03-cli-design.md`.** Když se plán a specifikace rozejdou, platí specifikace a rozpor nahlas oznam.
- **Každý `throw` má test, který tvrdí jeho přesnou hlášku.**
- **`docs/workflows/donut/` je referenční zátěž.** `tests/Donut/acceptance.rewrite.phpt` tvrdí, že ji parser a validátor projdou s **0 chybami a 0 varováními**. Task 1 ji přejmenovává; každý další task ji musí nechat na pokoji.

## Na čem se staví (hotové)

- `Donut\Template` — `render(array $map): string`, `getKeys()`, statická `isKeyName()`
- `Donut\Format\Workflow` — `$name`, `$description`, `$inputs` (`array<string, Input>`), `$steps`
- `Donut\Format\Input` — `$name`, `$required`, `$default`, `$description`
- `Donut\BlockRepository` — `__construct(string $directory)`, `has()`, `get()`, `getNames()`
- `Donut\Parser\WorkflowParser` — `parseFile(string $path): Workflow`, `parseArray(array $data, string $location): Workflow`
- `Donut\Parser\ParseException`
- `Donut\Runner\Runner` — `__construct(BlockRepository $blocks, ProcessRunner $processes, Reporter $reporter)`, `run(Workflow $w, array $initialMap = []): array`
- `Donut\Runner\NetteProcessRunner`, `ConsoleReporter` (`__construct($stream = null)`), `NullReporter`, `RunFailedException`

---

### Task 1: Přejmenování klíčů na camelCase

Mechanické, ale sahá do čtyř vrstev naráz. Proto samostatný task s vlastní recenzí, ne schované uvnitř práce na CLI.

**Files:**
- Modify: `docs/workflows/donut/blocks/*.json` (15 souborů)
- Modify: `docs/workflows/donut/workflows/*.json` (4 soubory)
- Modify: `src/Runner/Runner.php:184` — jméno kanálu
- Modify: `src/Validator/Validator.php:132`, `:157` — jméno kanálu
- Modify: `docs/format-specifikace.md` — doporučení v sekci 3, poznámka o optické kolizi v sekci 1
- Modify: testy, které používají `'STDIN'` jako klíč v `in`, a `tests/Donut/acceptance.rewrite.phpt`

**Interfaces:**
- Consumes: nic nového
- Produces: nic nového. Mění se jen jména dat a jedno jméno kanálu.

#### Tři kategorie jmen

| co to je | tvar | příklad |
|---|---|---|
| slovník formátu (klíče JSONu) | snake_case | `allow_failure`, `exit_code`, `stdin`, `type` |
| klíče mapy, které pojmenoval člověk | camelCase | `cardJson`, `shortId` |
| klíče mapy dodané enginem | VELKÁ | `STDIN`, `CWD` |

#### Co se v kódu mění a co ne

Mění se **jen tři místa**, a všechna jsou o `stdin` jako **jménu kanálu**:

- `src/Runner/Runner.php:184` — `isset($step->in['STDIN'])` → `isset($step->in['stdin'])` (obě výskyty na řádku)
- `src/Validator/Validator.php:132` — `if ($name === 'STDIN')` → `'stdin'`
- `src/Validator/Validator.php:157` — `!isset($step->in['STDIN'])` → `'stdin'`

**Nemění se** tato místa, protože jde o `STDIN` jako **klíč mapy** dodaný enginem:

- `src/Runner/Runner.php:99` — `$map + ['STDIN' => '', 'CWD' => $cwd]`
- `src/Validator/Validator.php:44` — `new KeyFlow([… 'STDIN', 'CWD'])`
- `src/Validator/Validator.php:182` — kontrola `{%STDIN%}` v `args`
- `src/Validator/Validator.php:208` — výjimka pro `STDIN` v kontrole nedeklarovaných proměnných v `args`

- [ ] **Step 1: Zapsat mapování jmen**

Vytvoř `/tmp/donut-rename.php` (pomocný soubor, necommituje se):

```php
<?php

declare(strict_types=1);

return [
	'ASSIST_BOARDS' => 'assistBoards',
	'BOARD' => 'board',
	'BOARD_ID' => 'boardId',
	'BOARDS_JSON' => 'boardsJson',
	'BRANCH' => 'branch',
	'CARD_ID' => 'cardId',
	'CARD_JSON' => 'cardJson',
	'CARDS' => 'cards',
	'CARDS_JSON' => 'cardsJson',
	'COMMENT' => 'comment',
	'COMMENT_JSON' => 'commentJson',
	'CURLRC' => 'curlrc',
	'DESCRIPTION' => 'description',
	'DEV_BOARDS' => 'devBoards',
	'EXPECT_STATUS' => 'expectStatus',
	'FILTER' => 'filter',
	'FLAGS' => 'flags',
	'LIST_ID' => 'listId',
	'LISTS_JSON' => 'listsJson',
	'ME_JSON' => 'meJson',
	'MESSAGE' => 'message',
	'METHOD' => 'method',
	'MODEL' => 'model',
	'MOVE_BODY' => 'moveBody',
	'MY_ID' => 'myId',
	'PATH' => 'path',
	'PROJECT' => 'project',
	'PROMPT' => 'prompt',
	'PROMPTS_DIR' => 'promptsDir',
	'PR_URL' => 'prUrl',
	'QUEUE_EXISTS' => 'queueExists',
	'QUEUE_FILE' => 'queueFile',
	'REPO' => 'repo',
	'RESULT' => 'agentResult',
	'SHORT_ID' => 'shortId',
	'STATUS' => 'status',
	'SYSTEM_PROMPT' => 'systemPrompt',
	'TAG' => 'tag',
	'TARGET_LIST' => 'targetList',
	'TARGET_LIST_ID' => 'targetListId',
	'TASK' => 'task',
	'TASK_ID' => 'taskId',
	'TEXT' => 'text',
	'TITLE' => 'title',
	'URL' => 'url',
	'WORK_DIR' => 'workDir',
	'WORKFLOW' => 'workflow',
	'WORK_ROOT' => 'workRoot',
];
```

`RESULT` se nepřepisuje na `result`, ale na **`agentResult`** — `result` je jméno kanálu a `"out": { "result": "result" }` by se četlo jako chyba, i když by fungovalo.

- [ ] **Step 2: Ověřit, že mapování pokrývá všechna jména v přepisu**

Run:

```bash
diff <(grep -rho '{%[A-Za-z0-9_]*%}' docs/workflows/donut | tr -d '{}%' | sort -u) \
     <(php -r 'foreach (require "/tmp/donut-rename.php" as $k => $v) echo "$k\n";' | sort)
```

Expected: žádný rozdíl. Kdyby nějaké jméno chybělo nebo přebývalo, oprav mapování — **neupravuj přepis, aby mapování sedělo**.

- [ ] **Step 3: Přejmenovat přepis**

Nahrazují se **dva přesné tvary**, nic jiného:

1. `{%STARE%}` → `{%nove%}`
2. řetězec, který je jménu roven **celý**: `"STARE"` → `"nove"`

**Nikdy podle hranic slova.** Popis kamene `workspace-pr` obsahuje větu *„Vypíše URL, jen když PR sám vytvořil"* — náhrada podle hranic slova by ji zmrzačila. Rovnost celého řetězce to vylučuje.

```bash
php -r '
$map = require "/tmp/donut-rename.php";
$files = array_merge(
    glob("docs/workflows/donut/blocks/*.json"),
    glob("docs/workflows/donut/workflows/*.json")
);
$n = 0;
foreach ($files as $f) {
    $s = $orig = file_get_contents($f);
    foreach ($map as $old => $new) {
        $s = str_replace(["{%{$old}%}", "\"{$old}\""], ["{%{$new}%}", "\"{$new}\""], $s);
    }
    $s = str_replace("\"STDIN\":", "\"stdin\":", $s);
    if ($s !== $orig) { file_put_contents($f, $s); $n++; }
}
echo "upraveno {$n} souborů\n";
'
```

Poslední náhrada mění **jméno kanálu** v `in`. Píše se s dvojtečkou, aby zasáhla jen klíč a ne hodnotu.

- [ ] **Step 4: Ověřit přejmenování**

```bash
grep -rn '{%[A-Z_]*%}' docs/workflows/donut && echo "ZŮSTALO STARÉ JMÉNO" || echo "žádné staré šablony"
grep -rn '"STDIN"' docs/workflows/donut && echo "ZŮSTAL STARÝ KANÁL" || echo "kanál přejmenován"
for f in docs/workflows/donut/blocks/*.json docs/workflows/donut/workflows/*.json; do jq empty "$f" || echo "NEPLATNÝ JSON: $f"; done
grep -c 'Vypíše URL' docs/workflows/donut/blocks/workspace-pr.json
```

Expected: žádné staré šablony, kanál přejmenován, všechny soubory platný JSON, a `1` — tedy že se česká věta s „URL" nezměnila.

- [ ] **Step 5: Změnit tři místa v kódu**

`src/Runner/Runner.php:184`:

```php
		$stdin = isset($step->in['stdin']) ? $step->in['stdin']->render($map) : '';
```

`src/Validator/Validator.php:132`:

```php
			if ($name === 'stdin') {
```

`src/Validator/Validator.php:157`:

```php
		if ($block->stdin !== null && $block->stdin->required && !isset($step->in['stdin'])) {
```

Ostatní výskyty `STDIN` v obou souborech **nech být** — jde o klíč mapy, ne o jméno kanálu.

- [ ] **Step 6: Přejmenovat kanál a fixtury v testech**

Nejdřív kanál. Najdi testy, které ho plní starým jménem:

```bash
grep -rln "'STDIN'\|\"STDIN\"" tests/
```

V nich změň **jen** klíč `STDIN` uvnitř `in` na `stdin`. Klíč mapy `STDIN` — třeba v tvrzení o počáteční mapě nebo v kontrole `{%STDIN%}` v `args` — nech být.

Pak fixtury. Testy si vymýšlejí vlastní jména vstupů a klíčů (`TEXT`, `URL`, `A`, `B`, `RC`, `FILTER`…). Přepiš je na camelCase (`text`, `url`, `a`, `b`, `rc`, `filter`), ať testy neučí čtenáře starý tvar — z testů se konvence opisuje stejně jako z dokumentace.

Jsou to jen data testů, takže kritérium správnosti je jednoduché: **`make test` musí dál procházet a nic v tvrzeních se nesmí změnit** kromě samotných jmen. Kdyby některé tvrzení bylo potřeba upravit jinak než přejmenováním, zastav se a nahlas to — znamenalo by to, že jméno někde nese význam, o kterém nevíme.

- [ ] **Step 7: Srovnat přijímací test s novými jmény**

`tests/Donut/acceptance.rewrite.phpt` tvrdí přítomnost vstupů starým jménem:

```php
Assert::true(isset($cardDev->inputs['shortId']));
Assert::false($cardDev->inputs['curlrc']->required);
```

- [ ] **Step 8: Spustit celou sadu**

Run: `make test`
Expected: OK, 22 testů. Zvlášť `acceptance.rewrite.phpt` musí dál hlásit 0 chyb a 0 varování na referenční zátěži.

Kdyby validace začala hlásit chyby, **neupravuj přepis** — znamenalo by to, že přejmenování něco rozvázalo, a to je nález k nahlášení.

- [ ] **Step 9: Ověřit PHPStan**

Run: `vendor/bin/phpstan analyse`
Expected: `[OK] No errors`

- [ ] **Step 10: Srovnat specifikaci**

V `docs/format-specifikace.md`:

1. V sekci 3 nahraď doporučení o velkých písmenech tabulkou tří kategorií:

```markdown
- Klíče jsou case-sensitive. Doporučení:
  - **slovník formátu** (klíče JSONu) je snake_case: `allow_failure`, `exit_code`, `stdin`
  - **klíče, které pojmenuješ sám**, jsou camelCase: `cardJson`, `shortId`
  - **klíče dodané enginem** jsou velkými: `STDIN`, `CWD`

  Velká písmena tím značí „tohle jsi nepojmenoval ty".
```

2. V sekci 1 uprav ukázku plnění stdin a **odstraň poznámku o optické kolizi** — ta kolize přestala existovat:

```markdown
Krok ho plní stejně jako ostatní vstupy, pod jménem `stdin`:

```json
"in": { "filter": ".title", "stdin": "{%task%}" }
```

Vstup CLI volání se pak plní `"in": { "stdin": "{%STDIN%}" }` — vlevo jméno
kanálu, vpravo klíč, který do mapy dodal engine.
```

3. Ukázky v sekci 1 a 2, které používají stará jména (`{%CURLRC%}`, `{%URL%}`, `{%ENV%}`, `{%RC%}`, `{%STATUS%}`, `{%TASK%}`, `{%FLAGS%}`, `{%FILTER%}`, `{%VERZE%}`), přepiš na camelCase.

- [ ] **Step 11: Commit**

```bash
git add docs src tests
git commit -m "Přejmenovat klíče mapy na camelCase

Velká písmena byla doporučená kvůli jednoznakovým delimiterům; s {%…%}
ten důvod padl. Zůstávají jen dvěma klíčům, které dodává engine — tím
začnou něco znamenat.

Jméno kanálu v in se mění z STDIN na stdin, čímž mizí optická kolize,
na kterou měla specifikace vlastní poznámku."
```

---

### Task 2: Runner — stdout na terminál a rozlišení nespuštění

Dvě změny v hotovém runneru, obě plynou z návrhu CLI.

**Files:**
- Create: `src/Runner/CannotStartException.php`
- Modify: `src/Runner/ProcessRunner.php` — signatura
- Modify: `src/Runner/NetteProcessRunner.php` — `$captureStdout`
- Modify: `src/Runner/Runner.php` — volání, dvě `throw`
- Modify: `tests/Donut/NetteProcessRunner.phpt`, `tests/Donut/Runner.linear.phpt`, `tests/Donut/Runner.branching.phpt`, `tests/Donut/Runner.acceptance.phpt` — falešné implementace mají novou signaturu
- Modify: `docs/format-specifikace.md` — sekce 2, kanály

**Interfaces:**
- Consumes: vše hotové
- Produces:
  - `Donut\Runner\CannotStartException extends RunFailedException`
  - `ProcessRunner::run(string $command, list<string> $args, string $stdin, bool $captureStdout, bool $captureStderr, ?int $timeout): ProcessResult`
  - `ProcessResult::$stdout` je nově `?string` — `null`, když se streamoval

- [ ] **Step 1: Napsat padající test na streamování stdoutu**

Do `tests/Donut/NetteProcessRunner.phpt` přidej:

```php
// stdout se streamuje, když ho volající nechce do paměti
$r = $runner->run('echo', ['ven'], '', false, false, 10);
Assert::null($r->stdout);
Assert::same(0, $r->exitCode);
```

A uprav **všechna stávající volání** v tom souboru tak, aby měla `true` jako čtvrtý argument (`$captureStdout`), protože dosud stdout zachytávala.

- [ ] **Step 2: Spustit test, ověřit že padá**

Run: `vendor/bin/tester -C tests/Donut/NetteProcessRunner.phpt`
Expected: FAIL — `run()` bere pět argumentů, ne šest.

- [ ] **Step 3: Rozšířit rozhraní a implementaci**

`src/Runner/ProcessRunner.php` — doplň parametr **před** `$captureStderr` a popiš ho:

```php
	 * @param  list<string> $args
	 * @param  bool $captureStdout true = do paměti, false = streamovat na terminál
	 * @param  bool $captureStderr true = do paměti, false = streamovat na terminál
	 * @param  ?int $timeout sekundy; null vypíná limit
	 * @throws \Nette\Utils\ProcessTimeoutException
	 * @throws \Nette\Utils\ProcessFailedException proces se vůbec nepodařilo spustit
	 */
	public function run(
		string $command,
		array $args,
		string $stdin,
		bool $captureStdout,
		bool $captureStderr,
		?int $timeout,
	): ProcessResult;
```

`src/Runner/ProcessResult.php` — `$stdout` je nově `?string`:

```php
	public function __construct(
		public readonly ?string $stdout,
		public readonly ?string $stderr,
		public readonly int $exitCode,
	) {
	}
```

`src/Runner/NetteProcessRunner.php`:

```php
		$process = Process::runExecutable(
			executable: $command,
			arguments: $args,
			stdin: $stdin,
			stdout: $captureStdout ? null : STDOUT,
			stderr: $captureStderr ? null : STDERR,
			timeout: $timeout === null ? null : (float) $timeout,
		);

		$exitCode = $process->getExitCode();

		return new ProcessResult(
			stdout: $captureStdout ? self::trimTrailingNewlines($process->getStdOutput()) : null,
			stderr: $captureStderr ? self::trimTrailingNewlines($process->getStdError()) : null,
			exitCode: $exitCode,
		);
```

- [ ] **Step 4: Spustit test, ověřit že prochází**

Run: `vendor/bin/tester -C tests/Donut/NetteProcessRunner.phpt`
Expected: PASS

- [ ] **Step 5: Napsat padající test na chování Runneru**

Do `tests/Donut/Runner.linear.phpt` přidej:

```php
// krok bez result ve výstupu si stdout nezachytává
$procs = new FakeProcesses([new ProcessResult(null, null, 0)]);
$run([
	'name' => 'w',
	'inputs' => ['t' => []],
	'steps' => [['type' => 'run', 'block' => 'echo', 'in' => ['text' => '{%t%}']]],
], $procs, ['t' => 'x']);
Assert::false($procs->calls[0][3]);

// krok s result ve výstupu si ho zachytává
$procs = new FakeProcesses([new ProcessResult('v', null, 0)]);
$run([
	'name' => 'w',
	'inputs' => ['t' => []],
	'steps' => [[
		'type' => 'run', 'block' => 'echo',
		'in' => ['text' => '{%t%}'],
		'out' => ['result' => 'r'],
	]],
], $procs, ['t' => 'x']);
Assert::true($procs->calls[0][3]);
```

Uprav `FakeProcesses::run()` na novou signaturu a zaznamenávej i `$captureStdout`, takže `$calls[N]` bude
`[$command, $args, $stdin, $captureStdout, $captureStderr, $timeout]`. Stávající tvrzení o `$procs->calls[0]` v tom souboru doplň o `true` na čtvrté pozici.

- [ ] **Step 6: Spustit test, ověřit že padá**

Run: `vendor/bin/tester -C tests/Donut/Runner.linear.phpt`
Expected: FAIL — `Runner` volá `run()` s pěti argumenty.

- [ ] **Step 7: Upravit Runner**

V `src/Runner/Runner.php` v metodě, která spouští krok, doplň vedle `$captureStderr`:

```php
		$captureStdout = isset($step->out['result']);
		$captureStderr = isset($step->out['stderr']);
```

a předej ho do volání jako čtvrtý argument. V zápisu kanálů do mapy ošetři, že `stdout` může být `null`:

```php
				'result' => $result->stdout ?? '',
```

- [ ] **Step 8: Spustit testy**

Run: `make test`
Expected: OK. Kdyby padly `Runner.branching.phpt` nebo `Runner.acceptance.phpt`, jde o falešné implementace se starou signaturou — oprav je, ale **neměň, co testy tvrdí**.

- [ ] **Step 9: Napsat padající test na CannotStartException**

Do `tests/Donut/Runner.linear.phpt`:

```php
// neúspěšná validace: běh se nespustil vůbec
$procs = new FakeProcesses;
Assert::exception(
	fn() => $run(['name' => 'w', 'steps' => [['type' => 'run', 'block' => 'neexistuje']]], $procs),
	Donut\Runner\CannotStartException::class
);
Assert::same([], $procs->calls);

// chybějící povinný vstup workflow: taky se nespustil
Assert::exception(
	fn() => $run([
		'name' => 'w',
		'inputs' => ['nutny' => ['required' => true]],
		'steps' => [['type' => 'set', 'key' => 'v', 'value' => '{%nutny%}']],
	], new FakeProcesses),
	Donut\Runner\CannotStartException::class,
	'w.json: povinný vstup "nutny" nemá hodnotu.'
);

// selhání kroku zůstává RunFailedException, ne CannotStartException
$procs = new FakeProcesses([new ProcessResult('', null, 3)]);
$e = Assert::exception(
	fn() => $run([
		'name' => 'w',
		'inputs' => ['t' => []],
		'steps' => [['type' => 'run', 'block' => 'echo', 'in' => ['text' => '{%t%}']]],
	], $procs, ['t' => 'x']),
	Donut\Runner\RunFailedException::class
);
Assert::false($e instanceof Donut\Runner\CannotStartException);
```

- [ ] **Step 10: Spustit test, ověřit že padá**

Run: `vendor/bin/tester -C tests/Donut/Runner.linear.phpt`
Expected: FAIL, `Class 'Donut\Runner\CannotStartException' not found`

- [ ] **Step 11: Napsat CannotStartException a použít ji**

`src/Runner/CannotStartException.php`:

```php
<?php

declare(strict_types=1);

namespace Donut\Runner;


/**
 * Běh se nespustil vůbec — workflow neprošlo validací, nebo mu chybí
 * povinný vstup. Neproběhl ani jeden krok.
 *
 * Podtřída, ne samostatný typ, aby volající, kterého ten rozdíl nezajímá,
 * dál chytal jen RunFailedException.
 */
final class CannotStartException extends RunFailedException
{
}
```

V `src/Runner/Runner.php` změň **dvě** `throw` na `CannotStartException` — tu po neúspěšné validaci (`Statická validace neprošla:`) a tu u chybějícího povinného vstupu (`povinný vstup … nemá hodnotu.`). Hlášky nech beze změny.

Ostatní `throw` v souboru **nech být** — všechny nastávají až za běhu.

- [ ] **Step 12: Spustit testy a PHPStan**

Run: `make test` → OK, `vendor/bin/phpstan analyse` → `[OK] No errors`

- [ ] **Step 13: Srovnat specifikaci**

V `docs/format-specifikace.md`, sekce 2, za tabulku kanálů doplň:

```markdown
Standardní výstup se řídí stejným pravidlem jako chybový: **zachytí se, jen
když si ho krok vyžádá do `out`**. Jinak teče na terminál. Bez toho by krok
nad `echo` nevypsal nic a workflow by nemělo jak dát člověku vědět.
```

- [ ] **Step 14: Commit**

```bash
git add src tests docs
git commit -m "Runner: stdout kroku bez out teče na terminál, CannotStartException

Stdout se dosud zachytával vždy a bez out se zahodil, takže repo-check
nevypsal nic, ačkoliv je to jeho jediný smysl. Nově platí stejné pravidlo
jako u stderr.

CannotStartException rozlišuje 'neproběhl ani jeden krok' od selhání za
běhu — CLI z toho vrací 2 místo 1."
```

---

### Task 3: Cli\Arguments

Rozklad `$argv`. Čistá funkce, testovatelná bez čehokoliv dalšího.

**Files:**
- Create: `src/Cli/Arguments.php`
- Create: `src/Cli/UsageException.php`
- Test: `tests/Donut/Cli.Arguments.phpt`

**Interfaces:**
- Consumes: `Donut\Exception`
- Produces:
  - `Donut\Cli\UsageException extends Donut\Exception`
  - `Donut\Cli\Arguments::parse(array<int, string> $argv): self`
  - veřejné readonly `?string $workflow`, `array<string, string> $values`, `bool $help`, `bool $list`

- [ ] **Step 1: Napsat padající test**

`tests/Donut/Cli.Arguments.phpt`:

```php
<?php

declare(strict_types=1);

use Donut\Cli\Arguments;
use Donut\Cli\UsageException;
use Tester\Assert;

require __DIR__ . '/../bootstrap.php';

// první prvek argv je jméno programu a ignoruje se
$a = Arguments::parse(['donut', 'card-dev', '--shortId=abc', '--model=sonnet']);
Assert::same('card-dev', $a->workflow);
Assert::same(['shortId' => 'abc', 'model' => 'sonnet'], $a->values);
Assert::false($a->help);
Assert::false($a->list);

// hodnota smí obsahovat rovnítko i mezery
$a = Arguments::parse(['donut', 'w', '--url=https://x/?a=1&b=2', '--message=ahoj svete']);
Assert::same(['url' => 'https://x/?a=1&b=2', 'message' => 'ahoj svete'], $a->values);

// prázdná hodnota je platná
$a = Arguments::parse(['donut', 'w', '--tag=']);
Assert::same(['tag' => ''], $a->values);

// příznaky
$a = Arguments::parse(['donut', '--list']);
Assert::true($a->list);
Assert::null($a->workflow);

$a = Arguments::parse(['donut', 'card-dev', '--help']);
Assert::true($a->help);
Assert::same('card-dev', $a->workflow);

$a = Arguments::parse(['donut', '--help']);
Assert::true($a->help);
Assert::null($a->workflow);

// holé volání
$a = Arguments::parse(['donut']);
Assert::null($a->workflow);
Assert::false($a->help);
Assert::false($a->list);
Assert::same([], $a->values);

// zopakovaný argument je chyba
Assert::exception(
	fn() => Arguments::parse(['donut', 'w', '--tag=a', '--tag=b']),
	UsageException::class,
	'Argument --tag je uvedený víckrát.'
);

// tvar bez rovnítka se nepodporuje
Assert::exception(
	fn() => Arguments::parse(['donut', 'w', '--tag', 'a']),
	UsageException::class,
	'Argument --tag musí mít tvar --tag=hodnota.'
);

// dvě jména workflow
Assert::exception(
	fn() => Arguments::parse(['donut', 'w', 'x']),
	UsageException::class,
	'Workflow je uvedené víckrát: "w" a "x".'
);

// jednopomlčkový tvar se nepodporuje
Assert::exception(
	fn() => Arguments::parse(['donut', 'w', '-t=a']),
	UsageException::class,
	'Neznámý argument "-t=a".'
);
```

- [ ] **Step 2: Spustit test, ověřit že padá**

Run: `vendor/bin/tester -C tests/Donut/Cli.Arguments.phpt`
Expected: FAIL, `Class 'Donut\Cli\Arguments' not found`

- [ ] **Step 3: Napsat UsageException**

`src/Cli/UsageException.php`:

```php
<?php

declare(strict_types=1);

namespace Donut\Cli;

use Donut\Exception;


/**
 * Volání z příkazové řádky nedává smysl — chybný tvar argumentu, neznámé
 * workflow, chybějící jméno. Běh se nespustí.
 */
final class UsageException extends Exception
{
}
```

- [ ] **Step 4: Napsat Arguments**

`src/Cli/Arguments.php`:

```php
<?php

declare(strict_types=1);

namespace Donut\Cli;


/**
 * Rozklad příkazové řádky.
 *
 * Podporuje se jen tvar --klic=hodnota. Tvar se dvěma slovy specifikace
 * neuvádí a jednoznačnost je tu cennější než pohodlí.
 */
final class Arguments
{
	/** @param array<string, string> $values */
	private function __construct(
		public readonly ?string $workflow,
		public readonly array $values,
		public readonly bool $help,
		public readonly bool $list,
	) {
	}


	/**
	 * @param  array<int, string> $argv první prvek je jméno programu
	 * @throws UsageException
	 */
	public static function parse(array $argv): self
	{
		$workflow = null;
		$values = [];
		$help = false;
		$list = false;

		foreach (\array_slice($argv, 1) as $arg) {
			if ($arg === '--help') {
				$help = true;

			} elseif ($arg === '--list') {
				$list = true;

			} elseif (\str_starts_with($arg, '--')) {
				$name = \substr($arg, 2);
				$position = \strpos($name, '=');

				if ($position === false) {
					throw new UsageException("Argument --{$name} musí mít tvar --{$name}=hodnota.");
				}

				$key = \substr($name, 0, $position);

				if (isset($values[$key])) {
					throw new UsageException("Argument --{$key} je uvedený víckrát.");
				}

				$values[$key] = \substr($name, $position + 1);

			} elseif (\str_starts_with($arg, '-')) {
				throw new UsageException("Neznámý argument \"{$arg}\".");

			} elseif ($workflow !== null) {
				throw new UsageException("Workflow je uvedené víckrát: \"{$workflow}\" a \"{$arg}\".");

			} else {
				$workflow = $arg;
			}
		}

		return new self($workflow, $values, $help, $list);
	}
}
```

- [ ] **Step 5: Spustit test, ověřit že prochází**

Run: `vendor/bin/tester -C tests/Donut/Cli.Arguments.phpt`
Expected: PASS

- [ ] **Step 6: Ověřit make test a PHPStan**

Run: `make test` → OK, `vendor/bin/phpstan analyse` → `[OK] No errors`

- [ ] **Step 7: Commit**

```bash
git add src/Cli tests/Donut/Cli.Arguments.phpt
git commit -m "Cli\\Arguments: rozklad příkazové řádky"
```

---

### Task 4: Cli\Application

Složí repozitáře, rozhodne mezi `--list`, `--help` a během, přeloží výjimky na návratové kódy.

**Files:**
- Create: `src/Cli/Application.php`
- Test: `tests/Donut/Cli.Application.phpt`

**Interfaces:**
- Consumes: `Cli\Arguments`, `Cli\UsageException`, `BlockRepository`, `WorkflowParser`, `ParseException`, `Runner`, `NetteProcessRunner`, `ConsoleReporter`, `RunFailedException`, `CannotStartException`
- Produces:
  - `Donut\Cli\Application::__construct(string $directory, $stdout = null, $stderr = null, ?string $stdin = null)`
  - `Donut\Cli\Application::run(array<int, string> $argv): int` — návratový kód
  - konstanty `Application::Success = 0`, `Application::Failed = 1`, `Application::NotStarted = 2`

`$stdin` je předaný obsah standardního vstupu, nebo `null` = přečíst si ho sám. Testy předávají řetězec, `bin/donut` předává `null`.

- [ ] **Step 1: Napsat padající test**

`tests/Donut/Cli.Application.phpt`:

```php
<?php

declare(strict_types=1);

use Donut\Cli\Application;
use Nette\Utils\FileSystem;
use Tester\Assert;

require __DIR__ . '/../bootstrap.php';

$dir = TEMP_DIR . '/home';
FileSystem::createDir($dir . '/blocks');
FileSystem::createDir($dir . '/workflows');

file_put_contents($dir . '/blocks/echo.json', json_encode([
	'name' => 'echo', 'command' => 'echo',
	'args' => [['{%text%}']],
	'inputs' => ['text' => ['required' => true]],
]));

file_put_contents($dir . '/blocks/fail.json', json_encode([
	'name' => 'fail', 'command' => '/usr/bin/false', 'args' => [],
]));

file_put_contents($dir . '/workflows/pozdrav.json', json_encode([
	'name' => 'pozdrav',
	'description' => 'Pozdraví.',
	'inputs' => [
		'kdo' => ['required' => true, 'description' => 'Koho pozdravit'],
		'tag' => ['required' => false, 'default' => 'ahoj', 'description' => 'Pozdrav'],
	],
	'steps' => [[
		'type' => 'run', 'block' => 'echo',
		'in' => ['text' => '{%tag%} {%kdo%}'],
	]],
]));

file_put_contents($dir . '/workflows/spadne.json', json_encode([
	'name' => 'spadne',
	'description' => 'Vždycky selže.',
	'steps' => [['type' => 'run', 'block' => 'fail']],
]));

/**
 * Pozn.: zachytí se jen to, co píše Application — tedy --list, --help
 * a chybové hlášky. Standardní výstup spuštěných kroků jde na skutečný
 * STDOUT procesu, ne do podstrčeného streamu, takže se tady ověřit nedá.
 * Od toho je přijímací test v Cli.acceptance.phpt, který pouští donut jako
 * samostatný proces přes proc_open.
 *
 * @return array{int, string, string} kód, stdout, stderr
 */
function spust(string $dir, array $argv): array
{
	$out = fopen('php://memory', 'r+');
	$err = fopen('php://memory', 'r+');
	$code = (new Application($dir, $out, $err, ''))->run($argv);
	rewind($out);
	rewind($err);
	$result = [$code, stream_get_contents($out), stream_get_contents($err)];
	fclose($out);
	fclose($err);

	return $result;
}


// --list vypíše workflow s popisem, abecedně
[$code, $out] = spust($dir, ['donut', '--list']);
Assert::same(0, $code);
Assert::contains('pozdrav', $out);
Assert::contains('Pozdraví.', $out);
Assert::contains('spadne', $out);
Assert::true(strpos($out, 'pozdrav') < strpos($out, 'spadne'));

// nápověda k workflow vypíše vstupy, povinnost i popis
[$code, $out] = spust($dir, ['donut', 'pozdrav', '--help']);
Assert::same(0, $code);
Assert::contains('--kdo=', $out);
Assert::contains('povinný', $out);
Assert::contains('Koho pozdravit', $out);
Assert::contains('--tag=', $out);
Assert::contains('volitelný', $out);

// běh doběhne, kód 0
[$code] = spust($dir, ['donut', 'pozdrav', '--kdo=svete']);
Assert::same(0, $code);

// selhání kroku je kód 1
[$code, , $err] = spust($dir, ['donut', 'spadne']);
Assert::same(1, $code);
Assert::contains('exit code 1', $err);

// chybějící povinný vstup je kód 2
[$code, , $err] = spust($dir, ['donut', 'pozdrav']);
Assert::same(2, $code);
Assert::contains('povinný vstup "kdo" nemá hodnotu', $err);

// neexistující workflow je kód 2
[$code, , $err] = spust($dir, ['donut', 'neexistuje']);
Assert::same(2, $code);
Assert::contains('neexistuje', $err);

// neznámý argument je kód 2
[$code, , $err] = spust($dir, ['donut', 'pozdrav', '--kdo=x', '--neznamy=y']);
Assert::same(2, $code);
Assert::contains('neznamy', $err);

// holé volání vypíše použití a skončí dvojkou
[$code, $out] = spust($dir, ['donut']);
Assert::same(2, $code);
Assert::contains('donut', $out);

// --help bez workflow vypíše použití a skončí nulou
[$code, $out] = spust($dir, ['donut', '--help']);
Assert::same(0, $code);
Assert::contains('--list', $out);

FileSystem::delete(TEMP_DIR);
```

- [ ] **Step 2: Spustit test, ověřit že padá**

Run: `vendor/bin/tester -C tests/Donut/Cli.Application.phpt`
Expected: FAIL, `Class 'Donut\Cli\Application' not found`

- [ ] **Step 3: Napsat Application**

`src/Cli/Application.php`:

```php
<?php

declare(strict_types=1);

namespace Donut\Cli;

use Donut\BlockRepository;
use Donut\Format\Workflow;
use Donut\Parser\ParseException;
use Donut\Parser\WorkflowParser;
use Donut\Runner\CannotStartException;
use Donut\Runner\ConsoleReporter;
use Donut\Runner\NetteProcessRunner;
use Donut\Runner\RunFailedException;
use Donut\Runner\Runner;


/**
 * Vstupní bod z příkazové řádky.
 *
 * Adresář a streamy bere v konstruktoru, aby šla testovat bez skutečného
 * terminálu — stejný důvod, proč má Reporter rozhraní.
 */
final class Application
{
	public const Success = 0;
	public const Failed = 1;
	public const NotStarted = 2;

	/** @var resource */
	private $stdout;

	/** @var resource */
	private $stderr;


	/**
	 * @param resource|null $stdout
	 * @param resource|null $stderr
	 * @param string|null   $stdin obsah standardního vstupu; null = přečíst si ho sám
	 */
	public function __construct(
		private readonly string $directory,
		$stdout = null,
		$stderr = null,
		private readonly ?string $stdin = null,
	) {
		$this->stdout = $stdout ?? STDOUT;
		$this->stderr = $stderr ?? STDERR;
	}


	/**
	 * @param array<int, string> $argv
	 */
	public function run(array $argv): int
	{
		try {
			$arguments = Arguments::parse($argv);

			if ($arguments->list) {
				return $this->listWorkflows();
			}

			if ($arguments->workflow === null) {
				$this->printUsage();

				return $arguments->help ? self::Success : self::NotStarted;
			}

			$workflow = $this->loadWorkflow($arguments->workflow);

			if ($arguments->help) {
				return $this->printWorkflowHelp($workflow);
			}

			return $this->runWorkflow($workflow, $arguments->values);

		} catch (UsageException | ParseException $e) {
			\fwrite($this->stderr, "Chyba: {$e->getMessage()}\n");

			return self::NotStarted;

		} catch (CannotStartException $e) {
			\fwrite($this->stderr, "Chyba: {$e->getMessage()}\n");

			return self::NotStarted;

		} catch (RunFailedException $e) {
			\fwrite($this->stderr, "Chyba: {$e->getMessage()}\n");

			return self::Failed;
		}
	}


	private function listWorkflows(): int
	{
		foreach ($this->workflowNames() as $name) {
			$workflow = $this->loadWorkflow($name);
			\fwrite($this->stdout, \sprintf("  %-12s %s\n", $name, $workflow->description ?? ''));
		}

		return self::Success;
	}


	private function printWorkflowHelp(Workflow $workflow): int
	{
		\fwrite($this->stdout, "{$workflow->name} — " . ($workflow->description ?? '') . "\n");

		if ($workflow->inputs !== []) {
			\fwrite($this->stdout, "\nVstupy:\n");

			foreach ($workflow->inputs as $name => $input) {
				\fwrite($this->stdout, \sprintf(
					"  --%-16s %-10s %s\n",
					$name . '=…',
					$input->required ? 'povinný' : 'volitelný',
					$input->description ?? '',
				));
			}
		}

		return self::Success;
	}


	/**
	 * @param array<string, string> $values
	 */
	private function runWorkflow(Workflow $workflow, array $values): int
	{
		foreach (\array_keys($values) as $name) {
			if (!isset($workflow->inputs[$name])) {
				throw new UsageException(
					"Workflow \"{$workflow->name}\" nezná vstup \"{$name}\"."
				);
			}
		}

		$blocks = new BlockRepository($this->directory . '/blocks');
		$runner = new Runner($blocks, new NetteProcessRunner, new ConsoleReporter($this->stderr));

		$runner->run($workflow, $values + ['STDIN' => $this->readStdin()]);

		return self::Success;
	}


	private function loadWorkflow(string $name): Workflow
	{
		$path = $this->directory . '/workflows/' . $name . '.json';

		if (!\is_file($path)) {
			throw new UsageException("Workflow \"{$name}\" neexistuje.");
		}

		return (new WorkflowParser)->parseFile($path);
	}


	/**
	 * @return array<int, string> abecedně
	 */
	private function workflowNames(): array
	{
		$paths = \glob($this->directory . '/workflows/*.json');
		$names = [];

		foreach ($paths === false ? [] : $paths as $path) {
			$names[] = \basename($path, '.json');
		}

		\sort($names);

		return $names;
	}


	private function readStdin(): string
	{
		if ($this->stdin !== null) {
			return $this->stdin;
		}

		if (\stream_isatty(STDIN)) {
			return '';
		}

		return (string) \stream_get_contents(STDIN);
	}


	private function printUsage(): void
	{
		\fwrite($this->stdout, <<<'TEXT'
			donut --list                          seznam workflow
			donut <workflow> --help               nápověda k workflow
			donut <workflow> [--klic=hodnota …]   spuštění

			Kameny a workflow se hledají v ./blocks a ./workflows.

			TEXT);
	}
}
```

- [ ] **Step 4: Spustit test, ověřit že prochází**

Run: `vendor/bin/tester -C tests/Donut/Cli.Application.phpt`
Expected: PASS

- [ ] **Step 5: Ověřit make test a PHPStan**

Run: `make test` → OK, `vendor/bin/phpstan analyse` → `[OK] No errors`

- [ ] **Step 6: Commit**

```bash
git add src/Cli/Application.php tests/Donut/Cli.Application.phpt
git commit -m "Cli\\Application: seznam, nápověda, běh a návratové kódy"
```

---

### Task 5: bin/donut a přijímací test skutečným procesem

Bez tohohle tasku zůstane neověřené právě to, co dělá z knihovny nástroj.

**Files:**
- Create: `bin/donut`
- Modify: `composer.json` — klíč `bin`
- Test: `tests/Donut/Cli.acceptance.phpt`

**Interfaces:**
- Consumes: `Cli\Application`
- Produces: spustitelný `bin/donut`

- [ ] **Step 1: Napsat bin/donut**

`bin/donut`:

```php
#!/usr/bin/env php
<?php

declare(strict_types=1);

foreach ([__DIR__ . '/../vendor/autoload.php', __DIR__ . '/../../../autoload.php'] as $file) {
	if (\is_file($file)) {
		require $file;

		break;
	}
}

exit((new Donut\Cli\Application(\getcwd() ?: '.'))->run($argv));
```

Dvě cesty k autoloaderu proto, že balíček může být nainstalovaný jako závislost — pak `bin/donut` leží ve `vendor/donut-org/donut/bin/`.

- [ ] **Step 2: Nastavit práva a composer**

```bash
chmod +x bin/donut
```

V `composer.json` přidej za `"autoload"`:

```json
	"bin": ["bin/donut"]
```

- [ ] **Step 3: Ověřit ručně**

```bash
cd docs/workflows/donut && php ../../../bin/donut --list; cd -
```

Expected: čtyři workflow s popisem. Kdyby to spadlo, je to nález — nahlas ho, neupravuj přepis.

- [ ] **Step 4: Napsat přijímací test**

`tests/Donut/Cli.acceptance.phpt`:

```php
<?php

declare(strict_types=1);

use Nette\Utils\FileSystem;
use Tester\Assert;

require __DIR__ . '/../bootstrap.php';

$dir = TEMP_DIR . '/home';
FileSystem::createDir($dir . '/blocks');
FileSystem::createDir($dir . '/workflows');

file_put_contents($dir . '/blocks/echo.json', json_encode([
	'name' => 'echo', 'command' => 'echo',
	'args' => [['{%text%}']],
	'inputs' => ['text' => ['required' => true]],
]));

file_put_contents($dir . '/blocks/upper.json', json_encode([
	'name' => 'upper', 'command' => 'tr',
	'args' => [['a-z', 'A-Z']],
	'stdin' => ['required' => true],
]));

file_put_contents($dir . '/workflows/hlasite.json', json_encode([
	'name' => 'hlasite',
	'description' => 'Zvětší, co dostane.',
	'inputs' => ['kdo' => ['required' => true]],
	'steps' => [
		[
			'type' => 'run', 'block' => 'echo',
			'in' => ['text' => 'ahoj {%kdo%}'],
			'out' => ['result' => 'pozdrav'],
		],
		[
			'type' => 'run', 'block' => 'upper',
			'in' => ['stdin' => '{%pozdrav%}'],
		],
	],
]));

$bin = escapeshellarg(__DIR__ . '/../../bin/donut');

/** @return array{int, string, string} */
function donut(string $dir, string $bin, string $args): array
{
	$descriptors = [1 => ['pipe', 'w'], 2 => ['pipe', 'w']];
	$process = proc_open("php {$bin} {$args}", $descriptors, $pipes, $dir);
	Assert::type('resource', $process);
	$out = (string) stream_get_contents($pipes[1]);
	$err = (string) stream_get_contents($pipes[2]);
	fclose($pipes[1]);
	fclose($pipes[2]);

	return [proc_close($process), $out, $err];
}


// skutečný proces: běh projde a poslední krok vypíše na stdout
[$code, $out] = donut($dir, $bin, 'hlasite --kdo=svete');
Assert::same(0, $code);
Assert::contains('AHOJ SVETE', $out);

// seznam
[$code, $out] = donut($dir, $bin, '--list');
Assert::same(0, $code);
Assert::contains('hlasite', $out);
Assert::contains('Zvětší, co dostane.', $out);

// nápověda
[$code, $out] = donut($dir, $bin, 'hlasite --help');
Assert::same(0, $code);
Assert::contains('--kdo=', $out);

// chybějící povinný vstup: kód 2, hláška na stderr, na stdout nic
[$code, $out, $err] = donut($dir, $bin, 'hlasite');
Assert::same(2, $code);
Assert::same('', $out);
Assert::contains('kdo', $err);

// neznámý argument: kód 2
[$code, , $err] = donut($dir, $bin, 'hlasite --kdo=x --neznamy=y');
Assert::same(2, $code);
Assert::contains('neznamy', $err);

FileSystem::delete(TEMP_DIR);
```

Pozn.: `proc_open` s pracovním adresářem je tu na místě — ověřuje se právě
to, že se kameny hledají v pracovním adresáři. Není to spouštění shellem
v produkčním kódu, kterého se zákaz týká.

- [ ] **Step 5: Spustit test**

Run: `vendor/bin/tester -C tests/Donut/Cli.acceptance.phpt`
Expected: PASS

- [ ] **Step 6: Ověřit celou sadu a PHPStan**

Run: `make test` → OK, `vendor/bin/phpstan analyse` → `[OK] No errors`

- [ ] **Step 7: Commit**

```bash
git add bin composer.json tests/Donut/Cli.acceptance.phpt
git commit -m "bin/donut a přijímací test skutečným procesem"
```

---

## Co tenhle plán vědomě nedělá

- **`--dry-run`.** `CommandLine` je samostatná jednotka právě proto, aby to šlo doplnit bez zásahu do `Runner`u. Teď ne.
- **Vlastní umístění kamenů.** Pracovní adresář stačí; kdyby přestal, přibude proměnná prostředí.
- **Barvy, `--quiet`, `--verbose`.** Průběh na stderr a výsledek na stdout stačí.
- **Přepsání `olw-*` skriptů** podle rozhraní v návrhu přepisu. Další práce.
