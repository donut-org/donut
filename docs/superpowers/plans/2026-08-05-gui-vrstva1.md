# GUI vrstva 1 — implementační plán

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Čtecí Nette aplikace v `gui/`, která nad tím, co je na disku, ukazuje validaci u konkrétních kroků a přehled kamenů s jejich vstupy.

**Architecture:** `gui/` je samostatný composer balíček, který na donut závisí přes path repository — nesahá do `../src`. Nic nezapisuje, nic necachuje: při každém requestu přečte `blocks/` a `workflows/` z pracovního adresáře a zvaliduje. Problémy se ke krokům přiřadí přes řetězec `Problem::$location`, který obě strany skládají stejným pravidlem.

**Tech Stack:** PHP 8.1+, `nette/application` 3.x, Latte, `nette/tester`. Donut jako závislost.

## Global Constraints

- **PHP 8.1+**, tabulátory jako odsazení, `declare(strict_types=1);` v každém PHP souboru, dvě prázdné řádky mezi metodami — přesně jako donut.
- **Uživatelské texty česky**, kód a identifikátory anglicky.
- **PSR-4 `Donut\Gui\` → `gui/src/`.** Jmenný prostor je zvolený tak, aby přesun do samostatného repozitáře `donut-org/donut-gui` byl přesun, ne přejmenování.
- **GUI nesmí sáhnout do `../src` ani do `../vendor`** relativní cestou. Donut je závislost, ne soused. Jediná zmínka o cestě `..` smí být v path repository v `gui/composer.json`.
- **Vrstva 1 nic nezapisuje.** Žádný `file_put_contents`, žádný serializér. Zápis přijde ve vrstvě 3.
- **Žádný cache, žádný stav.** Soubory se čtou a validují při každém requestu.
- **`blocks/` a `workflows/` se hledají v pracovním adresáři serveru**, stejně jako u CLI.
- **Bez databáze**, bez Docker, bez build kroku.
- Donut se v tomhle plánu mění **jen v Tasku 1** a jen o test. Žádný jeho zdrojový soubor se neupravuje.
- Testy donutu (`vendor/bin/tester tests -C`, 25 testů) i PHPStan level max musí zůstat zelené.
- V pracovním stromu donutu jsou tři nesledované položky (`.github/workflows/frontbot.yml`, `donut-org_donut.sublime-workspace`, `rss`). `git add` s konkrétními cestami, nikdy `git add -A` ani `git add .`.

## Struktura souborů

```
gui/
  composer.json          závislost na donutu přes path repository
  www/index.php          front controller
  config/common.neon     mapování presenterů a router
  src/Bootstrap.php      Nette Configurator
  src/StepPath.php       skládá cestu ke kroku stejně jako Validator
  src/ProblemMap.php     problémy z Result indexované podle cesty
  src/Presentation/Workflow/WorkflowPresenter.php
  src/Presentation/Workflow/default.latte    seznam workflow
  src/Presentation/Workflow/detail.latte     kroky s problémy
  src/Presentation/Block/BlockPresenter.php
  src/Presentation/Block/default.latte       přehled kamenů
  tests/                 nette/tester, jako v donutu
```

`StepPath` a `ProblemMap` jsou jediné dvě jednotky s logikou. Zbytek jsou
presentery, které přečtou disk a předají objekty šabloně.

---

### Task 1: Připnout tvar `Problem::$location` v donutu

`Problem::$location` je řetězec, který skládá `Validator::checkSteps()`.
GUI podle něj přiřazuje problémy ke krokům a **skládá si tentýž řetězec sám**.
Je to smlouva mezi dvěma repozitáři, kterou dnes nic nedrží: kdyby někdo
změnil oddělovač nebo jméno segmentu, donutu by testy prošly a GUI by tiše
přestalo problémy zobrazovat.

Tenhle task je **v repozitáři donutu**, ne v `gui/`.

**Files:**
- Create: `tests/Donut/Validator.location.phpt`

**Interfaces:**
- Consumes: `Donut\Validator\Validator`, `Donut\Validator\Result`, `Donut\Validator\Problem`, `Donut\Parser\WorkflowParser`, `Donut\BlockRepository`.
- Produces: připnuté tvary, na kterých staví Task 3:
  - krok nejvyšší úrovně → `<jméno>.json:steps[<i>]`
  - uvnitř `then` → `<jméno>.json:steps[<i>].then[<j>]`
  - uvnitř `else` → `<jméno>.json:steps[<i>].else[<j>]`
  - uvnitř těla `foreach` → `<jméno>.json:steps[<i>].steps[<j>]`
  - problém, který nepatří kroku → `<jméno>.json` (bez dvojtečky)

- [ ] **Step 1: Napiš padající test**

Vytvoř `tests/Donut/Validator.location.phpt`:

```php
<?php

declare(strict_types=1);

use Donut\BlockRepository;
use Donut\Parser\WorkflowParser;
use Donut\Validator\Validator;
use Tester\Assert;

require __DIR__ . '/../bootstrap.php';

// Tvar location je smlouva s GUI: to si tutéž cestu skládá samo, aby vědělo,
// ke kterému kroku problém patří. Kdyby se tvar změnil, GUI by tiše přestalo
// problémy zobrazovat — proto se připíná tady, ne tam.

$dir = TEMP_DIR . '/blocks';
Nette\Utils\FileSystem::createDir($dir);

$repo = new BlockRepository($dir);
$parser = new WorkflowParser;
$validator = new Validator($repo);

/** @return list<string> */
$locations = function (array $data) use ($parser, $validator): array {
	$result = $validator->validate($parser->parseArray($data, 'w.json'));
	return array_map(fn($p) => $p->location, $result->getProblems());
};

// Krok s neplatným jménem klíče dá problém právě u toho kroku, ve kterém je —
// proto se jím dá tvar cesty proměřit ve všech čtyřech pozicích naráz.
$found = $locations([
	'name' => 'w',
	'inputs' => ['t' => []],
	'steps' => [
		['type' => 'set', 'key' => 'A-B', 'value' => 'x'],
		[
			'type' => 'if',
			'condition' => ['left' => '{%t%}', 'op' => 'not_empty'],
			'then' => [['type' => 'set', 'key' => 'C-D', 'value' => 'x']],
			'else' => [['type' => 'set', 'key' => 'E-F', 'value' => 'x']],
		],
		[
			'type' => 'foreach',
			'over' => '{%t%}',
			'as' => 'radek',
			'steps' => [['type' => 'set', 'key' => 'G-H', 'value' => 'x']],
		],
	],
]);

Assert::contains('w.json:steps[0]', $found);
Assert::contains('w.json:steps[1].then[0]', $found);
Assert::contains('w.json:steps[1].else[0]', $found);
Assert::contains('w.json:steps[2].steps[0]', $found);

// Problém, který nepatří žádnému kroku, má cestu bez dvojtečky — GUI podle
// toho pozná, že ho má vypsat u workflow, ne u kroku.
$workflowLevel = $locations([
	'name' => 'w',
	'inputs' => ['nepouzity' => []],
	'steps' => [],
]);

Assert::same(['w.json'], $workflowLevel);
```

- [ ] **Step 2: Spusť test a ověř, že prochází**

Run: `cd /home/honza/Dokumenty/Projekty/donut-org/donut && vendor/bin/tester tests/Donut/Validator.location.phpt -C`
Expected: PASS.

Tenhle test **nemá padat** — připíná chování, které už existuje. Kdyby padl,
tvar cesty je jiný, než plán předpokládá: **zastav se, zapiš skutečné tvary
do reportu a nahlas to.** Task 3 na nich staví a stavěl by na písku.

- [ ] **Step 3: Ověř, že test doopravdy něco drží**

Zkus mutaci: v `src/Validator/Validator.php` v `checkSteps()` změň
`"{$at}.then"` na `"{$at}.THEN"` a spusť test znovu.

Run: `vendor/bin/tester tests/Donut/Validator.location.phpt -C`
Expected: FAIL na tvrzení o `then`.

Pak **mutaci vrať** (`git checkout src/Validator/Validator.php`) a ověř, že
je strom čistý: `git status --porcelain src/`.

- [ ] **Step 4: Spusť celou sadu a PHPStan**

Run: `vendor/bin/tester tests -C && vendor/bin/phpstan analyse`
Expected: 26 testů (bylo 25), PHPStan bez chyb.

- [ ] **Step 5: Commit**

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
git add tests/Donut/Validator.location.phpt
git commit -m "Připnout tvar Problem::location — je to smlouva s GUI"
```

---

### Task 2: Kostra aplikace a seznam workflow

Nette aplikace, která nastartuje, najde donut jako závislost a vypíše
workflow z pracovního adresáře.

**Files:**
- Create: `gui/composer.json`, `gui/www/index.php`, `gui/src/Bootstrap.php`, `gui/config/common.neon`
- Create: `gui/src/Presentation/Workflow/WorkflowPresenter.php`, `gui/src/Presentation/Workflow/default.latte`
- Create: `gui/.gitignore`

**Interfaces:**
- Consumes: `Donut\Parser\WorkflowParser::parseFile(string $path): Workflow`, `Donut\Format\Workflow` s veřejnými `$name`, `$description`, `$inputs`, `$steps`.
- Produces:
  - `Donut\Gui\Presentation\Workflow\WorkflowPresenter::renderDefault(): void` — akce `Workflow:default`
  - `Donut\Gui\Presentation\Workflow\WorkflowPresenter::projectDir(): string` — pracovní adresář, ze kterého se čte; použije ho i Task 5

- [ ] **Step 1: Založ balíček**

`gui/composer.json`:

```json
{
	"name": "donut-org/donut-gui",
	"description": "Autorské prostředí pro workflow donutu",
	"license": "BSD-3-Clause",
	"type": "project",
	"repositories": [
		{"type": "path", "url": "../"}
	],
	"require": {
		"php": ">=8.1",
		"donut-org/donut": "*",
		"nette/application": "^3.2"
	},
	"require-dev": {
		"nette/tester": "^2.6"
	},
	"autoload": {
		"psr-4": {"Donut\\Gui\\": "src/"}
	},
	"minimum-stability": "dev",
	"prefer-stable": true
}
```

`gui/.gitignore`:

```
/vendor
/temp
/composer.lock
```

`composer.lock` se necommituje schválně — path repository v něm drží absolutní
cestu k donutu, která platí jen na tomhle stroji.

Pozn.: `minimum-stability: dev` je potřeba proto, že donut zatím nemá tag —
path repository ho nabídne jako `dev-<větev>`.

- [ ] **Step 2: Nainstaluj a ověř, že donut je vidět jako balíček**

Run:
```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut/gui
composer install
php -r 'require "vendor/autoload.php"; echo get_class(new Donut\Parser\WorkflowParser), "\n";'
ls -l vendor/donut-org/donut
```
Expected: vypíše `Donut\Parser\WorkflowParser`, a `vendor/donut-org/donut` je
symlink do nadřazeného adresáře.

Kdyby `composer install` selhal na stabilitě nebo na tom, že path repository
nenajde balíček, **nezkoušej to obejít relativním `require`** — nahlas to.
Nesahat do `../src` je tvrdé omezení, ne preference.

- [ ] **Step 3: Front controller a bootstrap**

`gui/www/index.php`:

```php
<?php

declare(strict_types=1);

require __DIR__ . '/../vendor/autoload.php';

Donut\Gui\Bootstrap::boot()
	->createContainer()
	->getByType(Nette\Application\Application::class)
	->run();
```

`gui/src/Bootstrap.php`:

```php
<?php

declare(strict_types=1);

namespace Donut\Gui;

use Nette\Bootstrap\Configurator;


final class Bootstrap
{
	public static function boot(): Configurator
	{
		$root = \dirname(__DIR__);

		$configurator = new Configurator;
		$configurator->setDebugMode(true);
		$configurator->setTempDirectory($root . '/temp');
		$configurator->addConfig($root . '/config/common.neon');

		return $configurator;
	}
}
```

`gui/config/common.neon`:

```neon
application:
	mapping:
		*: Donut\Gui\Presentation\*\**Presenter

services:
	# SimpleRouter stačí: adresy jsou v query stringu
	# (?presenter=Workflow&action=detail&name=card-dev) a nic hezčího
	# lokální autorský nástroj nepotřebuje. Bez routeru by aplikace
	# nenastartovala — nette/application ho vyžaduje jako službu.
	- Nette\Application\Routers\SimpleRouter(Workflow:default)
```

Adresář `gui/temp/` musí existovat a být zapisovatelný — Nette si do něj
ukládá zkompilovaný DI kontejner a šablony. Založ ho:

```bash
mkdir -p /home/honza/Dokumenty/Projekty/donut-org/donut/gui/temp
```

Je v `.gitignore`, takže se necommituje; v readme (Task 6) na něj upozorni.

- [ ] **Step 4: Presenter a šablona**

`gui/src/Presentation/Workflow/WorkflowPresenter.php`:

```php
<?php

declare(strict_types=1);

namespace Donut\Gui\Presentation\Workflow;

use Donut\Format\Workflow;
use Donut\Parser\ParseException;
use Donut\Parser\WorkflowParser;
use Nette\Application\UI\Presenter;


/**
 * Workflow se čtou z pracovního adresáře serveru, stejně jako u CLI.
 * Nic se necachuje — soubor se čte při každém requestu.
 */
final class WorkflowPresenter extends Presenter
{
	public function renderDefault(): void
	{
		$this->template->workflows = $this->loadAll();
	}


	public static function projectDir(): string
	{
		return \getcwd() ?: '.';
	}


	/**
	 * @return array<string, Workflow|string> jméno => workflow, nebo hláška o chybě
	 */
	private function loadAll(): array
	{
		$paths = \glob(self::projectDir() . '/workflows/*.json');
		$loaded = [];

		foreach ($paths === false ? [] : $paths as $path) {
			$name = \basename($path, '.json');

			try {
				$loaded[$name] = (new WorkflowParser)->parseFile($path);

			} catch (ParseException $e) {
				// Vadný soubor nesmí schovat ostatní — stejné pravidlo jako
				// u `donut --list`.
				$loaded[$name] = $e->getMessage();
			}
		}

		\ksort($loaded);

		return $loaded;
	}
}
```

`gui/src/Presentation/Workflow/default.latte`:

```latte
{block content}
<h1>Workflow</h1>

<ul n:if="$workflows">
	<li n:foreach="$workflows as $name => $workflow">
		{if is_string($workflow)}
			<strong>{$name}</strong> — <span class=error>{$workflow}</span>
		{else}
			<a n:href="detail $name">{$name}</a>
			{$workflow->description}
		{/if}
	</li>
</ul>

<p n:if="!$workflows">V adresáři <code>{Donut\Gui\Presentation\Workflow\WorkflowPresenter::projectDir()}/workflows</code> nic není.</p>

<p><a n:href="Block:default">Kameny</a></p>
```

Šablona potřebuje `@layout.latte`. Vytvoř `gui/src/Presentation/@layout.latte`:

```latte
<!DOCTYPE html>
<html lang=cs>
<meta charset=utf-8>
<title>donut</title>
<style>
	body { font: 15px/1.5 system-ui, sans-serif; margin: 2rem; max-width: 60rem }
	.error { color: #a00 }
	.warning { color: #a60 }
	code { background: #f4f4f4; padding: .1em .3em }
</style>
{include content}
```

- [ ] **Step 5: Ověř, že aplikace běží**

Run:
```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut/docs/workflows/donut
php -S 127.0.0.1:8123 -t ../../../gui/www &
SERVER=$!
sleep 1
curl -s http://127.0.0.1:8123/ | tee /tmp/gui-out.html | grep -c 'card-dev\|card-spec\|repo-check\|sync'
kill $SERVER
```
Expected: `4` — všechna čtyři workflow z referenční zátěže jsou na stránce.
Když vyjde míň, podívej se do `/tmp/gui-out.html`, co se vypsalo.

Pozn.: server se spouští z `docs/workflows/donut/`, protože právě odtud se
mají `blocks/` a `workflows/` hledat. Adresář `-t` je jiný — to je kořen
dokumentů, ne projekt.

- [ ] **Step 6: Commit**

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
git add gui/composer.json gui/.gitignore gui/www gui/config gui/src
git commit -m "GUI: kostra Nette aplikace a seznam workflow"
```

Ověř `git status --porcelain gui/`, že se nepřidal `composer.lock`, `vendor/`
ani `temp/`.

---

### Task 3: `StepPath` a `ProblemMap`

Dvě jednotky, ve kterých je veškerá logika vrstvy 1. `StepPath` skládá cestu
ke kroku **stejným pravidlem jako `Validator::checkSteps()`**; `ProblemMap`
podle ní vytahuje problémy z `Result`.

Připomínka, proč to takhle: šablona se stromem kroků rekurzivně prochází a na
každé úrovni potřebuje vědět, které problémy patří tomuhle kroku. Cestu si
proto skládá cestou dolů a ptá se na ni. Tvar cesty připíná Task 1 v donutu.

**Files:**
- Create: `gui/src/StepPath.php`, `gui/src/ProblemMap.php`
- Create: `gui/tests/StepPath.phpt`, `gui/tests/ProblemMap.phpt`, `gui/tests/bootstrap.php`

**Interfaces:**
- Consumes: tvary cest připnuté Taskem 1; `Donut\Validator\Result::getProblems(): list<Problem>`; `Donut\Validator\Problem` s veřejnými `$severity`, `$location`, `$message` a konstantami `Problem::Error`, `Problem::Warning`.
- Produces:
  - `Donut\Gui\StepPath::root(string $workflowName): self` — `card-dev.json:steps`
  - `StepPath::index(int $i): self` — připojí `[7]`
  - `StepPath::child(string $property): self` — připojí `.then`
  - `StepPath::__toString(): string`
  - `Donut\Gui\ProblemMap::fromResult(Result $result): self`
  - `ProblemMap::at(StepPath|string $where): list<Problem>`

- [ ] **Step 1: Testovací bootstrap**

`gui/tests/bootstrap.php`:

```php
<?php

declare(strict_types=1);

require __DIR__ . '/../vendor/autoload.php';

Tester\Environment::setup();
```

- [ ] **Step 2: Napiš padající testy**

`gui/tests/StepPath.phpt`:

```php
<?php

declare(strict_types=1);

use Donut\Gui\StepPath;
use Tester\Assert;

require __DIR__ . '/bootstrap.php';

// Tvary musí odpovídat tomu, co skládá Validator::checkSteps() — připnuto
// v donutu testem tests/Donut/Validator.location.phpt.

Assert::same('card-dev.json:steps', (string) StepPath::root('card-dev'));

Assert::same(
	'card-dev.json:steps[7]',
	(string) StepPath::root('card-dev')->index(7)
);

Assert::same(
	'card-dev.json:steps[1].then[0]',
	(string) StepPath::root('card-dev')->index(1)->child('then')->index(0)
);

Assert::same(
	'card-dev.json:steps[1].else[0]',
	(string) StepPath::root('card-dev')->index(1)->child('else')->index(0)
);

Assert::same(
	'card-dev.json:steps[2].steps[0]',
	(string) StepPath::root('card-dev')->index(2)->child('steps')->index(0)
);

// Původní objekt se nemění — šablona prochází strom a jednu cestu větví
// do víc dětí.
$base = StepPath::root('w')->index(1);
$then = $base->child('then')->index(0);

Assert::same('w.json:steps[1]', (string) $base);
Assert::same('w.json:steps[1].then[0]', (string) $then);
```

`gui/tests/ProblemMap.phpt`:

```php
<?php

declare(strict_types=1);

use Donut\Gui\ProblemMap;
use Donut\Gui\StepPath;
use Donut\Validator\Problem;
use Donut\Validator\Result;
use Tester\Assert;

require __DIR__ . '/bootstrap.php';

$result = new Result;
$result->add(Problem::error('w.json:steps[0]', 'první'));
$result->add(Problem::warning('w.json:steps[0]', 'druhý'));
$result->add(Problem::error('w.json:steps[1].then[0]', 've větvi'));
$result->add(Problem::warning('w.json', 'k celému workflow'));

$map = ProblemMap::fromResult($result);

// Dva problémy u téhož kroku se oba vrátí, v pořadí, v jakém přišly.
$atZero = $map->at(StepPath::root('w')->index(0));
Assert::count(2, $atZero);
Assert::same('první', $atZero[0]->message);
Assert::same('druhý', $atZero[1]->message);

// Cesta se dá předat i jako řetězec.
Assert::count(2, $map->at('w.json:steps[0]'));

// Vnořený krok dostane svůj problém a nedostane cizí.
$atThen = $map->at(StepPath::root('w')->index(1)->child('then')->index(0));
Assert::count(1, $atThen);
Assert::same('ve větvi', $atThen[0]->message);

// Krok, který problém nemá, dostane prázdné pole, ne null.
Assert::same([], $map->at(StepPath::root('w')->index(9)));

// Problém k celému workflow se nesmí připlést k žádnému kroku.
Assert::count(1, $map->at('w.json'));
Assert::same('k celému workflow', $map->at('w.json')[0]->message);
```

- [ ] **Step 3: Spusť testy a ověř, že padají**

Run: `cd /home/honza/Dokumenty/Projekty/donut-org/donut/gui && vendor/bin/tester tests -C`
Expected: FAIL — `Donut\Gui\StepPath` ani `Donut\Gui\ProblemMap` neexistují.

- [ ] **Step 4: Napiš `StepPath`**

`gui/src/StepPath.php`:

```php
<?php

declare(strict_types=1);

namespace Donut\Gui;


/**
 * Cesta ke kroku ve tvaru, v jakém ji skládá Donut\Validator\Validator.
 *
 * Existuje proto, aby šablona při procházení stromu kroků uměla říct, které
 * problémy patří právě tomuhle kroku. Tvar je smlouva s donutem a je připnutý
 * jeho testem tests/Donut/Validator.location.phpt.
 *
 * Neměnná: index() ani child() nemění původní objekt, protože jedna cesta se
 * větví do víc dětí.
 */
final class StepPath implements \Stringable
{
	private function __construct(
		private readonly string $path,
	) {
	}


	public static function root(string $workflowName): self
	{
		return new self("{$workflowName}.json:steps");
	}


	public function index(int $i): self
	{
		return new self("{$this->path}[{$i}]");
	}


	/**
	 * @param string $property then, else nebo steps — jméno kolekce, do které
	 *                         se sestupuje
	 */
	public function child(string $property): self
	{
		return new self("{$this->path}.{$property}");
	}


	public function __toString(): string
	{
		return $this->path;
	}
}
```

- [ ] **Step 5: Napiš `ProblemMap`**

`gui/src/ProblemMap.php`:

```php
<?php

declare(strict_types=1);

namespace Donut\Gui;

use Donut\Validator\Problem;
use Donut\Validator\Result;


/**
 * Problémy z jedné validace, indexované podle cesty ke kroku.
 *
 * Problém, který nepatří žádnému kroku, má cestu bez dvojtečky (jen
 * `card-dev.json`) a vytáhne se stejným způsobem.
 */
final class ProblemMap
{
	/**
	 * @param array<string, list<Problem>> $byLocation
	 */
	private function __construct(
		private readonly array $byLocation,
	) {
	}


	public static function fromResult(Result $result): self
	{
		$byLocation = [];

		foreach ($result->getProblems() as $problem) {
			$byLocation[$problem->location][] = $problem;
		}

		return new self($byLocation);
	}


	/**
	 * @return list<Problem> v pořadí, v jakém je validátor ohlásil
	 */
	public function at(StepPath|string $where): array
	{
		return $this->byLocation[(string) $where] ?? [];
	}
}
```

- [ ] **Step 6: Spusť testy a ověř, že prochází**

Run: `cd /home/honza/Dokumenty/Projekty/donut-org/donut/gui && vendor/bin/tester tests -C`
Expected: PASS, 3 testy.

- [ ] **Step 7: Ověř, že se testy o tvar cesty opravdu opírají**

Zkus mutaci: v `gui/src/StepPath.php` změň v `child()` `"{$this->path}.{$property}"`
na `"{$this->path}/{$property}"` a spusť sadu.

Run: `vendor/bin/tester tests -C`
Expected: FAIL v `StepPath.phpt` na tvrzeních o `then`, `else` i `steps`.

Pak mutaci vrať a ověř `git status --porcelain gui/src`.

- [ ] **Step 8: Commit**

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
git add gui/src/StepPath.php gui/src/ProblemMap.php gui/tests
git commit -m "GUI: StepPath a ProblemMap"
```

---

### Task 4: Detail workflow — kroky s problémy u kroku

Jádro vrstvy 1. Strom kroků, u každého jeho problémy, a nahoře problémy, které
nepatří žádnému kroku.

**Files:**
- Modify: `gui/src/Presentation/Workflow/WorkflowPresenter.php` (přibude `renderDetail`)
- Create: `gui/src/Presentation/Workflow/detail.latte`
- Create: `gui/src/Presentation/Workflow/steps.latte` (rekurzivní blok)

**Interfaces:**
- Consumes: `StepPath` a `ProblemMap` z Tasku 3; `Donut\Validator\Validator::validate(Workflow $w): Result`; `Donut\BlockRepository::__construct(string $dir)`.
- Produces: akce `Workflow:detail` s parametrem `name`.

- [ ] **Step 1: Doplň `renderDetail` do presenteru**

Přidej do `WorkflowPresenter` (a doplň `use` pro `BlockRepository`,
`Validator`, `BadRequestException`, `ProblemMap`, `StepPath`):

```php
	public function renderDetail(string $name): void
	{
		$path = self::projectDir() . '/workflows/' . $name . '.json';

		if (!\is_file($path)) {
			$this->error("Workflow \"{$name}\" neexistuje.");
		}

		$workflow = (new WorkflowParser)->parseFile($path);
		$blocks = new BlockRepository(self::projectDir() . '/blocks');
		$result = (new Validator($blocks))->validate($workflow);

		$this->template->workflow = $workflow;
		$this->template->blocks = $blocks;
		$this->template->problems = ProblemMap::fromResult($result);
		$this->template->rootPath = StepPath::root($workflow->name);
		$this->template->workflowProblems = ProblemMap::fromResult($result)
			->at($workflow->name . '.json');
	}
```

- [ ] **Step 2: Rekurzivní blok pro kroky**

`gui/src/Presentation/Workflow/steps.latte`:

```latte
{define steps, array $steps, Donut\Gui\StepPath $path, Donut\Gui\ProblemMap $problems}
<ol>
	{foreach $steps as $i => $step}
		{var $at = $path->index($i)}
		<li>
			{if $step instanceof Donut\Format\RunStep}
				<strong>run</strong> {$step->block}
				{if $step->name} — {$step->name}{/if}
				<ul n:if="$step->in">
					<li n:foreach="$step->in as $key => $template">{$key}: <code>{$template->getSource()}</code></li>
				</ul>
				<ul n:if="$step->out">
					<li n:foreach="$step->out as $channel => $key">{$channel} → <code>{$key}</code></li>
				</ul>

			{elseif $step instanceof Donut\Format\SetStep}
				<strong>set</strong> <code>{$step->key}</code> = <code>{$step->value->getSource()}</code>

			{elseif $step instanceof Donut\Format\IfStep}
				<strong>if</strong>
				<code>{$step->condition->left->getSource()}</code>
				{$step->condition->op}
				{if $step->condition->right}<code>{$step->condition->right->getSource()}</code>{/if}

			{elseif $step instanceof Donut\Format\ForeachStep}
				<strong>foreach</strong>
				<code>{$step->over->getSource()}</code> as <code>{$step->as}</code>
			{/if}

			{foreach $problems->at($at) as $problem}
				<div class="{$problem->severity === Donut\Validator\Problem::Error ? 'error' : 'warning'}">
					{$problem->severity === Donut\Validator\Problem::Error ? '✕' : '⚠'} {$problem->message}
				</div>
			{/foreach}

			{if $step instanceof Donut\Format\IfStep}
				{if $step->then}
					then:
					{include steps, steps: $step->then, path: $at->child('then'), problems: $problems}
				{/if}
				{if $step->else}
					else:
					{include steps, steps: $step->else, path: $at->child('else'), problems: $problems}
				{/if}
			{elseif $step instanceof Donut\Format\ForeachStep}
				{include steps, steps: $step->steps, path: $at->child('steps'), problems: $problems}
			{/if}
		</li>
	{/foreach}
</ol>
{/define}
```

- [ ] **Step 3: Šablona detailu**

`gui/src/Presentation/Workflow/detail.latte`:

```latte
{import 'steps.latte'}

{block content}
<p><a n:href="default">← workflow</a></p>

<h1>{$workflow->name}</h1>
<p n:if="$workflow->description">{$workflow->description}</p>

<div n:foreach="$workflowProblems as $problem" class="{$problem->severity === Donut\Validator\Problem::Error ? 'error' : 'warning'}">
	{$problem->severity === Donut\Validator\Problem::Error ? '✕' : '⚠'} {$problem->message}
</div>

<h2 n:if="$workflow->inputs">Vstupy</h2>
<ul n:if="$workflow->inputs">
	<li n:foreach="$workflow->inputs as $input">
		<code>--{$input->name}=</code>
		{$input->required ? 'povinný' : 'volitelný'}
		{if $input->default !== null}(výchozí <code>{$input->default}</code>){/if}
		{$input->description}
	</li>
</ul>

<h2>Kroky</h2>
{include steps, steps: $workflow->steps, path: $rootPath, problems: $problems}
```

- [ ] **Step 4: Ověř nad referenční zátěží**

Referenční zátěž v `docs/workflows/donut/` má **nula chyb a nula varování** —
to hlídá přijímací test donutu. Na `card-dev` proto nesmí být vidět ani jedna
hláška, a přitom musí být vidět všech 27 kroků nejvyšší úrovně.

Run:
```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut/docs/workflows/donut
php -S 127.0.0.1:8123 -t ../../../gui/www &
SERVER=$!
sleep 1
curl -s 'http://127.0.0.1:8123/?presenter=Workflow&action=detail&name=card-dev' > /tmp/gui-card-dev.html
kill $SERVER

echo "chyb a varování (má být 0):"
grep -c 'class="error"\|class="warning"' /tmp/gui-card-dev.html || true
echo "kroků nejvyšší úrovně (má být 27):"
grep -o '<strong>\(run\|set\|if\|foreach\)</strong>' /tmp/gui-card-dev.html | wc -l
```
Expected: nula hlášek. Počet kroků bude **vyšší než 27** — 27 je jen nejvyšší
úroveň a `card-dev` má vnořené kroky v `if`. Zapiš skutečné číslo do reportu;
slouží jako referenční hodnota, ne jako tvrzení.

- [ ] **Step 5: Ověř, že se problémy zobrazují u správného kroku**

Rozbij dočasně kopii workflow tak, aby vzniklo varování ve vnořené větvi,
a ověř, že se ukáže tam, a ne nahoře:

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut/docs/workflows/donut
cp workflows/repo-check.json /tmp/repo-check.bak
python3 - <<'EOF'
import json
d=json.load(open('workflows/repo-check.json'))
# do then větve vloží set s neplatným jménem klíče
d['steps'][1]['then'].insert(0, {"type":"set","key":"A-B","value":"x"})
json.dump(d, open('workflows/repo-check.json','w'), ensure_ascii=False, indent=2)
EOF

php -S 127.0.0.1:8123 -t ../../../gui/www &
SERVER=$!
sleep 1
curl -s 'http://127.0.0.1:8123/?presenter=Workflow&action=detail&name=repo-check' > /tmp/gui-broken.html
kill $SERVER

cp /tmp/repo-check.bak workflows/repo-check.json
git status --porcelain workflows/
```

V `/tmp/gui-broken.html` musí být hláška `klíč "A-B" není platné jméno`
**uvnitř vnořeného seznamu** pod krokem `if`, ne v bloku nahoře nad kroky.
Ověř to očima a napiš do reportu, co jsi viděl. `git status` musí být prázdný —
soubor je vrácený.

- [ ] **Step 6: Commit**

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
git add gui/src/Presentation/Workflow
git commit -m "GUI: detail workflow s problémy u konkrétních kroků"
```

---

### Task 5: Přehled kamenů

Poslední kus vrstvy 1: co který kámen bere, jestli čte stdin a co spouští.

**Files:**
- Create: `gui/src/Presentation/Block/BlockPresenter.php`, `gui/src/Presentation/Block/default.latte`

**Interfaces:**
- Consumes: `Donut\BlockRepository::getNames(): array`, `::get(string $name): Block`; `Donut\Format\Block` s veřejnými `$name`, `$command`, `$args`, `$inputs`, `$stdin`, `$timeout`, `$allowFailure`, `$description`; `WorkflowPresenter::projectDir()`.
- Produces: akce `Block:default`.

- [ ] **Step 1: Presenter**

`gui/src/Presentation/Block/BlockPresenter.php`:

```php
<?php

declare(strict_types=1);

namespace Donut\Gui\Presentation\Block;

use Donut\BlockRepository;
use Donut\Gui\Presentation\Workflow\WorkflowPresenter;
use Donut\Parser\ParseException;
use Nette\Application\UI\Presenter;


final class BlockPresenter extends Presenter
{
	public function renderDefault(): void
	{
		$dir = WorkflowPresenter::projectDir() . '/blocks';

		try {
			$repository = new BlockRepository($dir);

		} catch (ParseException $e) {
			$this->template->blocks = [];
			$this->template->error = $e->getMessage();
			$this->template->dir = $dir;

			return;
		}

		$blocks = [];

		foreach ($repository->getNames() as $name) {
			$blocks[$name] = $repository->get($name);
		}

		$this->template->blocks = $blocks;
		$this->template->error = null;
		$this->template->dir = $dir;
	}
}
```

- [ ] **Step 2: Šablona**

`gui/src/Presentation/Block/default.latte`:

```latte
{block content}
<p><a n:href="Workflow:default">← workflow</a></p>

<h1>Kameny</h1>

<p n:if="$error" class=error>{$error}</p>
<p n:if="!$error && !$blocks">V adresáři <code>{$dir}</code> žádné kameny nejsou.</p>

<div n:foreach="$blocks as $block">
	<h2>{$block->name}</h2>
	<p n:if="$block->description">{$block->description}</p>

	<p>příkaz: <code>{$block->command}</code></p>

	<p n:if="$block->stdin">
		čte <strong>stdin</strong> —
		{$block->stdin->required ? 'povinný' : 'volitelný'}
		{$block->stdin->description}
	</p>

	<ul n:if="$block->inputs">
		<li n:foreach="$block->inputs as $input">
			<code>{$input->name}</code>
			{$input->required ? 'povinný' : 'volitelný'}
			{if $input->default !== null}(výchozí <code>{$input->default}</code>){/if}
			{$input->description}
		</li>
	</ul>

	<p n:if="$block->timeout !== null">timeout: {$block->timeout} s</p>
	<p n:if="$block->allowFailure !== false">
		allow_failure: {is_array($block->allowFailure) ? implode(', ', $block->allowFailure) : 'jakýkoliv exit kód'}
	</p>
</div>
```

- [ ] **Step 3: Ověř nad referenční zátěží**

Referenční zátěž má **15 kamenů** — to hlídá přijímací test donutu.

Run:
```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut/docs/workflows/donut
php -S 127.0.0.1:8123 -t ../../../gui/www &
SERVER=$!
sleep 1
curl -s 'http://127.0.0.1:8123/?presenter=Block&action=default' > /tmp/gui-blocks.html
kill $SERVER

echo "kamenů (má být 15):"
grep -c '^	<h2>' /tmp/gui-blocks.html || grep -c '<h2>' /tmp/gui-blocks.html
echo "kamenů, které čtou stdin (má být 6):"
grep -c 'čte <strong>stdin</strong>' /tmp/gui-blocks.html
```
Expected: 15 kamenů a 6 se stdin — `agent`, `curl-send-json`, `format-prompt`,
`jq`, `trello-to-task` a `workspace-pr`. Když některé číslo nesedí, nepřepisuj
očekávání: podívej se do `/tmp/gui-blocks.html`, co se doopravdy vypsalo,
a nahlas to.

- [ ] **Step 4: Spusť sadu GUI a testy donutu**

Run:
```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut/gui && vendor/bin/tester tests -C
cd /home/honza/Dokumenty/Projekty/donut-org/donut && vendor/bin/tester tests -C && vendor/bin/phpstan analyse
```
Expected: GUI 3 testy, donut 26 testů, PHPStan bez chyb.

- [ ] **Step 5: Commit**

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
git add gui/src/Presentation/Block
git commit -m "GUI: přehled kamenů"
```

---

### Task 6: Readme a zaznamenání v zadání

Vrstva 1 je použitelná — patří k ní návod, jak ji spustit, a odškrtnutí kroku 5.

**Files:**
- Create: `gui/readme.md`
- Modify: `docs/zadani.md` (bod 5 v „Pořadí prací")

**Interfaces:**
- Consumes: hotové vrstvy 1.
- Produces: nic.

- [ ] **Step 1: `gui/readme.md`**

Napiš krátké readme, **česky** (na rozdíl od kořenového readme donutu je tohle
interní nástroj, ne balíček na Packagistu). Musí obsahovat:

- co to je: autorské prostředí pro workflow a kameny, **jen pro čtení** —
  nic nezapisuje
- instalaci: `composer install` v `gui/`
- spuštění: `php -S 127.0.0.1:8000 -t <cesta>/gui/www`, spouštěné
  **z adresáře projektu**, kde jsou `blocks/` a `workflows/` — s příkladem
  pro `docs/workflows/donut/`
- co je vidět: seznam workflow, detail s kroky a problémy u konkrétního kroku,
  přehled kamenů
- co zatím není: zápis, tok klíčů, builder — a odkaz na
  `../docs/superpowers/specs/2026-08-05-gui-design.md`

Skutečné příkazy si ověř spuštěním, nepiš je od oka.

- [ ] **Step 2: Odškrtni krok 5 v zadání**

V `docs/zadani.md`, sekce „Pořadí prací", nahraď řádek

```
5. Teprve pak GUI
```

tímhle:

```markdown
5. GUI — autorské prostředí, staví se po vrstvách,
   viz `superpowers/specs/2026-08-05-gui-design.md` a `gui/`
   - ~~vrstva 1: validace u kroku a přehled kamenů~~ — hotovo, jen pro čtení
   - vrstva 2: tok klíčů — vyžaduje evidenci cest ve `Validator`u
   - vrstva 3: builder — vyžaduje serializér v donutu
```

Neodškrtávej celý bod 5 — hotová je jedna vrstva ze tří.

- [ ] **Step 3: Ověř odkazy**

Run:
```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
for p in docs/superpowers/specs/2026-08-05-gui-design.md gui/www gui/composer.json; do
	test -e "$p" && echo "OK  $p" || echo "CHYBÍ  $p"
done
```
Expected: třikrát `OK`.

- [ ] **Step 4: Commit**

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
git add gui/readme.md docs/zadani.md
git commit -m "GUI: readme a odškrtnutí kroku 5 v zadání"
```
