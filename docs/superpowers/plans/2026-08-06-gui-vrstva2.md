# GUI vrstva 2 — tok klíčů — implementační plán

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** V detailu workflow ukázat u každého kroku, které klíče čte a zapisuje, a kliknutím na klíč zvýraznit všechna místa, kde figuruje.

**Architecture:** Nová jednotka `Donut\Gui\KeyMap` projde strom workflow jednou a odpoví na otázky „co dělá tenhle krok s klíči" a „kde všude je tenhle klíč". Zvýraznění jde přes parametr `key` v adrese — server-side, bez JavaScriptu. Do donutu se sahá jen o dva gettery na `Result`, a to kvůli spojovacímu testu, ne kvůli funkci.

**Tech Stack:** PHP 8.3+ (gui), `nette/application` 3.3, Latte 3.1, `nette/tester`. Donut jako závislost přes path repository.

## Global Constraints

- PHP: donut `>=8.1`, gui `>=8.3`. **Tabulátory** jako odsazení, `declare(strict_types=1);` v každém PHP souboru, dvě prázdné řádky mezi metodami.
- **Uživatelské texty česky**, kód a identifikátory anglicky.
- **PSR-4 `Donut\Gui\` → `gui/src/`.**
- **GUI nesmí sáhnout do `../src` ani do `../vendor` relativní cestou.** Donut je závislost, ne soused. Jediná zmínka o `..` mimo `gui/` smí být path repository v `gui/composer.json`.
- **Vrstva 2 nic nezapisuje** do `blocks/` ani `workflows/`. Zápis přijde ve vrstvě 3.
- **Žádný cache, žádný stav** — soubory se čtou a validují při každém requestu.
- **Žádný JavaScript.**
- **`blocks/` a `workflows/` se hledají v pracovním adresáři serveru.**
- Zelené po každém commitu: v `gui/` `vendor/bin/tester tests -C` a `vendor/bin/phpstan analyse`; v kořeni `vendor/bin/tester tests -C` a `vendor/bin/phpstan analyse`.
- `git add` s konkrétními cestami, **nikdy** `git add -A` ani `git add .` — v pracovním stromu jsou tři nesledované položky (`.github/workflows/frontbot.yml`, `donut-org_donut.sublime-workspace`, `rss`), které do commitu nepatří.
- Referenční zátěž `docs/workflows/donut/` má **nula chyb a nula varování** — regresní etalon. Cokoliv v ní dočasně změníš, vrať.

## Jak GUI spustit

`php -S ... -t <docroot>` **bez router scriptu dělá `chdir()`** do docrootu, takže `getcwd()` vrátí `gui/www`, GUI hledá `blocks/` tam a **selže to tiše** (HTTP 200, „nic není"). Správně:

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut/docs/workflows/donut
php -S 127.0.0.1:8200 -t . /home/honza/Dokumenty/Projekty/donut-org/donut/gui/www/index.php &
```

Adresy jsou v query stringu; Nette kanonizuje výchozí presenter pryč, takže `curl -sL`. **Každý server, který spustíš, zabij.**

## Struktura souborů

```
src/Validator/Result.php          + getReadKeys(), getWrittenKeys(), setKeys()
src/Validator/Validator.php       validate() ty množiny předá do Result
tests/Donut/Validator.keys.phpt   nový

gui/src/KeyMap.php                nová jednotka — jediná s logikou
gui/tests/KeyMap.phpt             nový
gui/tests/KeyMap.ValidatorContract.phpt   nový — spojuje obě strany
gui/src/Presentation/Workflow/WorkflowPresenter.php        renderDetail bere key
gui/src/Presentation/Workflow/WorkflowDetailTemplate.php   + keys, selectedKey
gui/src/Presentation/Workflow/steps.latte                  klíče jako odkazy
gui/src/Presentation/Workflow/detail.latte                 předá nové proměnné
gui/src/Presentation/@layout.latte                         styly pro zvýraznění
gui/readme.md                                              popis vrstvy 2
docs/zadani.md                                             odškrtnout vrstvu 2
```

---

### Task 1: `Result` vydá množiny klíčů

`Validator::validate()` na konci obě množiny už má a zahodí je. Vystavují se
**jen kvůli spojovacímu testu v Tasku 3** — GUI je k vykreslení nepotřebuje.

**Files:**
- Modify: `src/Validator/Result.php`
- Modify: `src/Validator/Validator.php` (konec `validate()`)
- Create: `tests/Donut/Validator.keys.phpt`

**Interfaces:**
- Consumes: `Donut\Validator\KeyFlow::getRead(): array<int, string>` a `getWritten(): array<int, string>` — existují.
- Produces:
  - `Donut\Validator\Result::getReadKeys(): list<string>` — abecedně
  - `Donut\Validator\Result::getWrittenKeys(): list<string>` — abecedně
  - `Donut\Validator\Result::setKeys(array $read, array $written): void`

- [ ] **Step 1: Napiš padající test**

Vytvoř `tests/Donut/Validator.keys.phpt`:

```php
<?php

declare(strict_types=1);

use Donut\BlockRepository;
use Donut\Parser\WorkflowParser;
use Donut\Validator\Validator;
use Tester\Assert;

require __DIR__ . '/../bootstrap.php';

// Množiny klíčů vydává Result kvůli GUI: to si tutéž mapu odvozuje vlastním
// průchodem stromem a spojovací test v gui/ porovnává obojí. Bez toho by se
// ty dva průchody mohly rozejít a obě strany by zůstaly zelené.

$dir = TEMP_DIR . '/blocks';
Nette\Utils\FileSystem::createDir($dir);

file_put_contents($dir . '/echo.json', json_encode([
	'name' => 'echo',
	'command' => 'echo',
	'args' => [['{%text%}']],
	'inputs' => ['text' => ['required' => true]],
]));

$repo = new BlockRepository($dir);
$parser = new WorkflowParser;
$validator = new Validator($repo);

$workflow = $parser->parseArray([
	'name' => 'w',
	'inputs' => ['vstup' => []],
	'steps' => [
		// zápis přes set, čtení vstupu
		['type' => 'set', 'key' => 'zeSetu', 'value' => '{%vstup%}'],
		// zápis přes out, čtení klíče ze setu
		[
			'type' => 'run', 'block' => 'echo',
			'in' => ['text' => '{%zeSetu%}'],
			'out' => ['result' => 'zVystupu'],
		],
		// zápis přes foreach.as, čtení v over
		[
			'type' => 'foreach',
			'over' => '{%zVystupu%}',
			'as' => 'radek',
			'steps' => [
				['type' => 'run', 'block' => 'echo', 'in' => ['text' => '{%radek%}']],
			],
		],
	],
], 'w.json');

$result = $validator->validate($workflow);

// Zapisuje se ze všech tří míst, odkud zápis vzniká.
Assert::same(['radek', 'zVystupu', 'zeSetu'], $result->getWrittenKeys());

// Čte se ze šablon v set.value, run.in a foreach.over.
Assert::same(['radek', 'vstup', 'zVystupu', 'zeSetu'], $result->getReadKeys());

// Workflow bez jediného kroku má obě množiny prázdné, ne null.
$prazdne = $validator->validate($parser->parseArray(
	['name' => 'w', 'steps' => []],
	'w.json',
));

Assert::same([], $prazdne->getWrittenKeys());
Assert::same([], $prazdne->getReadKeys());
```

- [ ] **Step 2: Spusť test a ověř, že padá**

Run: `cd /home/honza/Dokumenty/Projekty/donut-org/donut && vendor/bin/tester tests/Donut/Validator.keys.phpt -C`
Expected: FAIL — `Result::getWrittenKeys()` neexistuje.

- [ ] **Step 3: Doplň `Result`**

Do `src/Validator/Result.php` přidej vlastnosti a tři metody. Vlož je za
`getWarnings()` a před `hasErrors()`:

```php
	/** @var list<string> */
	private array $readKeys = [];

	/** @var list<string> */
	private array $writtenKeys = [];
```

(vlastnosti patří nahoru k `$problems`)

```php
	/**
	 * Volá Validator na konci validate(). Jinam nepatří — Result je jinak
	 * jen sběrač problémů a tyhle množiny existují kvůli GUI, které si
	 * tutéž mapu odvozuje vlastním průchodem a testem se s tímhle porovnává.
	 *
	 * @param array<int, string> $read
	 * @param array<int, string> $written
	 */
	public function setKeys(array $read, array $written): void
	{
		\sort($read);
		\sort($written);

		$this->readKeys = \array_values($read);
		$this->writtenKeys = \array_values($written);
	}


	/** @return list<string> abecedně */
	public function getReadKeys(): array
	{
		return $this->readKeys;
	}


	/** @return list<string> abecedně */
	public function getWrittenKeys(): array
	{
		return $this->writtenKeys;
	}
```

- [ ] **Step 4: Naplň je ve `Validator::validate()`**

V `src/Validator/Validator.php`, v metodě `validate()`, těsně před `return $result;`
přidej:

```php
		$result->setKeys($read, $flow->getWritten());
```

Proměnná `$read` už v té metodě existuje (`$read = $flow->getRead();`).

- [ ] **Step 5: Spusť test a ověř, že prochází**

Run: `vendor/bin/tester tests/Donut/Validator.keys.phpt -C`
Expected: PASS.

Kdyby některé tvrzení nesedělo, **nepřepisuj očekávání** — vypiš si, co
`getReadKeys()` a `getWrittenKeys()` doopravdy vrací, a nahlas to. Task 3 na
těch množinách staví.

- [ ] **Step 6: Spusť celou sadu a PHPStan**

Run: `vendor/bin/tester tests -C && vendor/bin/phpstan analyse`
Expected: 27 testů (bylo 26), PHPStan bez chyb.

- [ ] **Step 7: Commit**

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
git add src/Validator/Result.php src/Validator/Validator.php tests/Donut/Validator.keys.phpt
git commit -m "Validator: Result vydá množiny čtených a zapisovaných klíčů"
```

---

### Task 2: `KeyMap`

Jediná jednotka vrstvy 2, která nese logiku. Projde strom workflow jednou
a zapamatuje si oba směry — klíč → cesty i cesta → klíče.

**Files:**
- Create: `gui/src/KeyMap.php`
- Create: `gui/tests/KeyMap.phpt`

**Interfaces:**
- Consumes: `Donut\Gui\StepPath::root(string $name): self`, `->index(int $i): self`, `->child(string $property): self`, `__toString()`. `Donut\Format\Workflow` s `$name` a `$steps`; `RunStep` s `$in` (`array<string, Template>`) a `$out` (`array<string, string>`); `SetStep` s `$key` a `$value`; `IfStep` s `$condition` (`->left`, `->right`); `ForeachStep` s `$over`, `$as`, `$steps`. `Donut\Template::getKeys(): array<int, string>`.
- Produces:
  - `Donut\Gui\KeyMap::of(Workflow $workflow): self`
  - `->writesAt(StepPath|string $path): list<string>` — jména klíčů, abecedně
  - `->readsAt(StepPath|string $path): list<string>`
  - `->writeSitesOf(string $key): list<string>` — cesty jako řetězce, v pořadí výskytu
  - `->readSitesOf(string $key): list<string>`
  - `->keys(): list<string>` — všechna jména, abecedně
  - `->classAt(StepPath|string $path, ?string $key): string` — `''`, `'write'`, `'read'` nebo `'write read'`

- [ ] **Step 1: Napiš padající test**

Vytvoř `gui/tests/KeyMap.phpt`:

```php
<?php

declare(strict_types=1);

use Donut\Gui\KeyMap;
use Donut\Gui\StepPath;
use Donut\Parser\WorkflowParser;
use Tester\Assert;

require __DIR__ . '/bootstrap.php';

$parser = new WorkflowParser;

$workflow = $parser->parseArray([
	'name' => 'w',
	'inputs' => ['vstup' => []],
	'steps' => [
		['type' => 'set', 'key' => 'zeSetu', 'value' => '{%vstup%}'],
		[
			'type' => 'run', 'block' => 'echo',
			'in' => ['text' => '{%zeSetu%} a {%vstup%}'],
			'out' => ['result' => 'zVystupu', 'exit_code' => 'kod'],
		],
		[
			'type' => 'if',
			'condition' => ['left' => '{%kod%}', 'op' => 'eq', 'right' => '{%vstup%}'],
			'then' => [['type' => 'set', 'key' => 'vetev', 'value' => '{%zVystupu%}']],
			'else' => [['type' => 'set', 'key' => 'vetev', 'value' => 'nic']],
		],
		[
			'type' => 'foreach',
			'over' => '{%zVystupu%}',
			'as' => 'radek',
			'steps' => [['type' => 'set', 'key' => 'vTele', 'value' => '{%radek%}']],
		],
	],
], 'w.json');

$map = KeyMap::of($workflow);
$root = StepPath::root('w');

// --- co dělá konkrétní krok ---

Assert::same(['zeSetu'], $map->writesAt($root->index(0)));
Assert::same(['vstup'], $map->readsAt($root->index(0)));

// out má dva kanály → dva zapsané klíče, abecedně
Assert::same(['kod', 'zVystupu'], $map->writesAt($root->index(1)));
// jedna šablona, dva klíče
Assert::same(['vstup', 'zeSetu'], $map->readsAt($root->index(1)));

// if čte v left i right a sám nic nezapisuje
Assert::same(['kod', 'vstup'], $map->readsAt($root->index(2)));
Assert::same([], $map->writesAt($root->index(2)));

// foreach zapisuje svoje `as`
Assert::same(['radek'], $map->writesAt($root->index(3)));
Assert::same(['zVystupu'], $map->readsAt($root->index(3)));

// krok, který s klíči nedělá nic, dá prázdná pole, ne null
Assert::same([], $map->writesAt($root->index(9)));
Assert::same([], $map->readsAt($root->index(9)));

// --- vnořené kroky ---

Assert::same(['vetev'], $map->writesAt($root->index(2)->child('then')->index(0)));
Assert::same(['zVystupu'], $map->readsAt($root->index(2)->child('then')->index(0)));
Assert::same(['vetev'], $map->writesAt($root->index(2)->child('else')->index(0)));
Assert::same([], $map->readsAt($root->index(2)->child('else')->index(0)));
Assert::same(['vTele'], $map->writesAt($root->index(3)->child('steps')->index(0)));

// --- kde všude klíč je ---

// vetev se zapisuje v obou větvích
Assert::same(
	[
		(string) $root->index(2)->child('then')->index(0),
		(string) $root->index(2)->child('else')->index(0),
	],
	$map->writeSitesOf('vetev'),
);

Assert::same(
	[(string) $root->index(0), (string) $root->index(1), (string) $root->index(2)],
	$map->readSitesOf('vstup'),
);

// vstup nikdo nezapisuje — je to vstup workflow
Assert::same([], $map->writeSitesOf('vstup'));

// klíč, který ve workflow není
Assert::same([], $map->writeSitesOf('neznamy'));
Assert::same([], $map->readSitesOf('neznamy'));

// --- seznam klíčů ---

Assert::same(
	['kod', 'radek', 'vTele', 'vetev', 'vstup', 'zVystupu', 'zeSetu'],
	$map->keys(),
);

// --- třída pro zvýraznění ---

Assert::same('', $map->classAt($root->index(0), null));
Assert::same('write', $map->classAt($root->index(0), 'zeSetu'));
Assert::same('read', $map->classAt($root->index(0), 'vstup'));
Assert::same('', $map->classAt($root->index(0), 'kod'));

// krok, který týž klíč čte i zapisuje, dostane obojí
$ctePise = KeyMap::of($parser->parseArray([
	'name' => 'w',
	'inputs' => ['x' => []],
	'steps' => [['type' => 'set', 'key' => 'x', 'value' => '{%x%}']],
], 'w.json'));

Assert::same('write read', $ctePise->classAt(StepPath::root('w')->index(0), 'x'));

// cesta jde předat i jako řetězec
Assert::same(['zeSetu'], $map->writesAt((string) $root->index(0)));
```

- [ ] **Step 2: Spusť test a ověř, že padá**

Run: `cd /home/honza/Dokumenty/Projekty/donut-org/donut/gui && vendor/bin/tester tests/KeyMap.phpt -C`
Expected: FAIL — `Donut\Gui\KeyMap` neexistuje.

- [ ] **Step 3: Napiš `KeyMap`**

Vytvoř `gui/src/KeyMap.php`:

```php
<?php

declare(strict_types=1);

namespace Donut\Gui;

use Donut\Format\ForeachStep;
use Donut\Format\IfStep;
use Donut\Format\RunStep;
use Donut\Format\SetStep;
use Donut\Format\Workflow;


/**
 * Kde který klíč vzniká a kdo ho čte.
 *
 * Odvozuje se z naparsovaného stromu, ne z validátoru — Template::getKeys()
 * je veřejná a víc než to potřeba není. Podmíněnost zápisu je vidět z cesty:
 * co obsahuje .then, .else nebo .steps, je uvnitř větve.
 *
 * Je to ale druhý průchod stromem vedle toho validátorova, takže by se ty dva
 * mohly rozejít — kdyby v donutu přibyl typ kroku nebo nové místo pro šablonu,
 * klíč by odsud tiše zmizel. Hlídá to KeyMap.ValidatorContract.phpt.
 */
final class KeyMap
{
	/**
	 * @param array<string, list<string>> $writesByKey  klíč => cesty
	 * @param array<string, list<string>> $readsByKey
	 * @param array<string, list<string>> $writesByPath cesta => klíče
	 * @param array<string, list<string>> $readsByPath
	 */
	private function __construct(
		private readonly array $writesByKey,
		private readonly array $readsByKey,
		private readonly array $writesByPath,
		private readonly array $readsByPath,
	) {
	}


	public static function of(Workflow $workflow): self
	{
		$writesByKey = [];
		$readsByKey = [];

		self::walk($workflow->steps, StepPath::root($workflow->name), $writesByKey, $readsByKey);

		return new self(
			$writesByKey,
			$readsByKey,
			self::invert($writesByKey),
			self::invert($readsByKey),
		);
	}


	/** @return list<string> jména klíčů, abecedně */
	public function writesAt(StepPath|string $path): array
	{
		return $this->writesByPath[(string) $path] ?? [];
	}


	/** @return list<string> jména klíčů, abecedně */
	public function readsAt(StepPath|string $path): array
	{
		return $this->readsByPath[(string) $path] ?? [];
	}


	/** @return list<string> cesty, v pořadí výskytu ve workflow */
	public function writeSitesOf(string $key): array
	{
		return $this->writesByKey[$key] ?? [];
	}


	/** @return list<string> cesty, v pořadí výskytu ve workflow */
	public function readSitesOf(string $key): array
	{
		return $this->readsByKey[$key] ?? [];
	}


	/** @return list<string> všechna jména klíčů, abecedně */
	public function keys(): array
	{
		$keys = \array_keys($this->writesByKey + $this->readsByKey);
		\sort($keys);

		return $keys;
	}


	/**
	 * Třída pro zvýraznění kroku. Krok může týž klíč číst i zapisovat
	 * (`set x = {%x%}`), pak dostane obojí.
	 */
	public function classAt(StepPath|string $path, ?string $key): string
	{
		if ($key === null) {
			return '';
		}

		$classes = [];

		if (\in_array($key, $this->writesAt($path), true)) {
			$classes[] = 'write';
		}

		if (\in_array($key, $this->readsAt($path), true)) {
			$classes[] = 'read';
		}

		return \implode(' ', $classes);
	}


	/**
	 * @param array<int, \Donut\Format\Step>  $steps
	 * @param array<string, list<string>>     $writes
	 * @param array<string, list<string>>     $reads
	 */
	private static function walk(array $steps, StepPath $path, array &$writes, array &$reads): void
	{
		foreach ($steps as $i => $step) {
			$here = $path->index($i);
			$at = (string) $here;

			if ($step instanceof RunStep) {
				// $step->in je klíčované jménem vstupu kamene; klíče mapy
				// jsou až v šablonách, které jsou jeho hodnotami.
				foreach ($step->in as $template) {
					foreach ($template->getKeys() as $key) {
						$reads[$key][] = $at;
					}
				}

				foreach ($step->out as $key) {
					$writes[$key][] = $at;
				}

			} elseif ($step instanceof SetStep) {
				foreach ($step->value->getKeys() as $key) {
					$reads[$key][] = $at;
				}

				$writes[$step->key][] = $at;

			} elseif ($step instanceof IfStep) {
				foreach ($step->condition->left->getKeys() as $key) {
					$reads[$key][] = $at;
				}

				if ($step->condition->right !== null) {
					foreach ($step->condition->right->getKeys() as $key) {
						$reads[$key][] = $at;
					}
				}

				self::walk($step->then, $here->child('then'), $writes, $reads);
				self::walk($step->else, $here->child('else'), $writes, $reads);

			} elseif ($step instanceof ForeachStep) {
				foreach ($step->over->getKeys() as $key) {
					$reads[$key][] = $at;
				}

				$writes[$step->as][] = $at;

				self::walk($step->steps, $here->child('steps'), $writes, $reads);
			}
		}
	}


	/**
	 * @param  array<string, list<string>> $byKey
	 * @return array<string, list<string>> cesta => klíče, abecedně a bez duplicit
	 */
	private static function invert(array $byKey): array
	{
		$byPath = [];

		foreach ($byKey as $key => $paths) {
			foreach ($paths as $path) {
				$byPath[$path][$key] = true;
			}
		}

		return \array_map(
			function (array $keys): array {
				$names = \array_keys($keys);
				\sort($names);

				return $names;
			},
			$byPath,
		);
	}
}
```

Pozn.: `writeSitesOf('vetev')` musí vrátit cesty **v pořadí výskytu**, proto se
`$writes[$key][]` plní během průchodu a nesortuje se. `writesAt()` naopak
vrací jména **abecedně**, což zajišťuje `invert()`.

Pozn. 2 (opraveno po code review — původní znění bylo neúplné): premisa, že
`Template::getKeys()` stačí, platí jen uvnitř jedné šablony. Nepočítala s tím,
že jeden krok umí odkazovat na týž klíč z **víc** šablon nebo kanálů zároveň —
dva vstupy kamene čtoucí stejný klíč, nebo dva kanály `out` mapující na
stejný klíč. Bez dedup na úrovni kroku by `writeSitesOf()`/`readSitesOf()`
vrátily tutéž cestu vícekrát. `walk()` proto dedupuje přes pomocnou metodu
`record()`, se sadou klíčů „už zapsaných tímhle krokem" resetovanou na
každou iteraci. Hlídají to case `dupCteni`/`dupZapis` v `KeyMap.phpt`.

- [ ] **Step 4: Spusť test a ověř, že prochází**

Run: `cd /home/honza/Dokumenty/Projekty/donut-org/donut/gui && vendor/bin/tester tests/KeyMap.phpt -C`
Expected: PASS.

- [ ] **Step 5: Ověř mutací, že test něco drží**

Zaveď do `gui/src/KeyMap.php` chybu a ověř, že sada spadne. Zkus **tři**:

1. v `walk()` u `RunStep` iteruj `$step->out` jako `$channel => $key` a zapisuj `$channel` místo `$key`
2. u `IfStep` vypusť průchod do `else`
3. v `classAt()` vrať `'read'` i pro zápis

Po každé mutaci ji vrať. Nakonec ověř `git status --porcelain gui/src`, že je
prázdný. Co některá mutace projde, napiš do reportu.

- [ ] **Step 6: Spusť sadu GUI a PHPStan**

Run: `cd /home/honza/Dokumenty/Projekty/donut-org/donut/gui && vendor/bin/tester tests -C && vendor/bin/phpstan analyse`
Expected: 8 testů (bylo 7), PHPStan bez chyb.

- [ ] **Step 7: Commit**

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
git add gui/src/KeyMap.php gui/tests/KeyMap.phpt
git commit -m "GUI: KeyMap — kde který klíč vzniká a kdo ho čte"
```

---

### Task 3: Spojovací test — `KeyMap` proti validátoru

`KeyMap` prochází strom **podruhé**, vedle validátoru. Kdyby v donutu přibyl
typ kroku nebo nové místo pro šablonu, GUI by o něm nevědělo, klíč by z mapy
tiše zmizel a **obě sady by zůstaly zelené**. Přesně to se v tomhle projektu
už jednou stalo u smlouvy o tvaru cesty.

**Files:**
- Create: `gui/tests/KeyMap.ValidatorContract.phpt`

**Interfaces:**
- Consumes: `Donut\Gui\KeyMap::of()` a `->keys()` z Tasku 2; `Donut\Validator\Result::getReadKeys()` a `getWrittenKeys()` z Tasku 1.
- Produces: nic.

- [ ] **Step 1: Napiš test**

Vytvoř `gui/tests/KeyMap.ValidatorContract.phpt`:

```php
<?php

declare(strict_types=1);

use Donut\BlockRepository;
use Donut\Gui\KeyMap;
use Donut\Parser\WorkflowParser;
use Donut\Validator\Validator;
use Tester\Assert;

require __DIR__ . '/bootstrap.php';

// Spojovací test smlouvy: KeyMap a Donut\Validator\Validator počítají tytéž
// množiny klíčů dvěma nezávislými průchody stromem. KeyMap.phpt ani donutí
// testy samy o sobě rozchod nepoznají — tenhle ano.
//
// Jede nad referenční zátěží, protože ta má všechny čtyři typy kroků, obě
// větve if i foreach, a hlídá ji přijímací test donutu na 0 chyb a 0 varování.

$root = __DIR__ . '/../../docs/workflows/donut';

$blocks = new BlockRepository($root . '/blocks');
$parser = new WorkflowParser;
$validator = new Validator($blocks);

$files = \glob($root . '/workflows/*.json');
Assert::count(4, $files, 'referenční zátěž má čtyři workflow');

foreach ($files as $file) {
	$workflow = $parser->parseFile($file);
	$result = $validator->validate($workflow);
	$map = KeyMap::of($workflow);

	$fromValidator = \array_unique([...$result->getReadKeys(), ...$result->getWrittenKeys()]);
	\sort($fromValidator);

	Assert::same(
		\array_values($fromValidator),
		$map->keys(),
		"množina klíčů v {$workflow->name} se musí shodovat s validátorem",
	);
}
```

Pozn.: test schválně sahá na `docs/workflows/donut/` relativní cestou. Je to
**testovací data, ne kód** — omezení „gui nesmí sáhnout mimo sebe" je o tom,
aby `gui/src` nezáviselo na umístění donutu. Až se `gui/` osamostatní, tenhle
test si s sebou vezme vlastní kopii fixtur; do té doby je referenční zátěž
lepší než ručně psaná náhražka, protože je hlídaná.

- [ ] **Step 2: Spusť test a ověř, že prochází**

Run: `cd /home/honza/Dokumenty/Projekty/donut-org/donut/gui && vendor/bin/tester tests/KeyMap.ValidatorContract.phpt -C`
Expected: PASS.

Kdyby spadl, **neupravuj test tak, aby prošel** — znamenalo by to, že se ty
dva průchody v něčem liší, a to je přesně nález, kvůli kterému test vzniká.
Vypiš si obě množiny, zjisti, který klíč chybí a kde, a nahlas to.

- [ ] **Step 3: Ověř, že rozchod doopravdy chytí**

Zaveď do `gui/src/KeyMap.php` v `walk()` u `ForeachStep` chybu — vypusť řádek
`$writes[$step->as][] = $at;` — a spusť sadu.

Run: `vendor/bin/tester tests -C`
Expected: FAIL v `KeyMap.ValidatorContract.phpt` — `sync` má `foreach`, takže
mu bude chybět klíč.

Pak mutaci vrať a ověř `git status --porcelain gui/src`, že je prázdný.

- [ ] **Step 4: Spusť sadu GUI a PHPStan**

Run: `cd /home/honza/Dokumenty/Projekty/donut-org/donut/gui && vendor/bin/tester tests -C && vendor/bin/phpstan analyse`
Expected: 9 testů (bylo 8), PHPStan bez chyb.

- [ ] **Step 5: Commit**

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
git add gui/tests/KeyMap.ValidatorContract.phpt
git commit -m "GUI: spojit KeyMap s validátorem testem nad referenční zátěží"
```

---

### Task 4: Klíče jako odkazy a zvýraznění

Jádro vrstvy 2. U každého kroku se vypíše, co čte a co zapisuje; klíče jsou
odkazy na tutéž stránku s `key` v adrese, a kroky, kde ten klíč figuruje, se
zvýrazní.

**Files:**
- Modify: `gui/src/Presentation/Workflow/WorkflowPresenter.php` (`renderDetail`)
- Modify: `gui/src/Presentation/Workflow/WorkflowDetailTemplate.php`
- Modify: `gui/src/Presentation/Workflow/steps.latte`
- Modify: `gui/src/Presentation/Workflow/detail.latte`
- Modify: `gui/src/Presentation/@layout.latte` (styly)

**Interfaces:**
- Consumes: `Donut\Gui\KeyMap::of()`, `->writesAt()`, `->readsAt()`, `->classAt()` z Tasku 2.
- Produces: akce `Workflow:detail` bere nepovinný parametr `key`.

- [ ] **Step 1: Rozšiř `WorkflowDetailTemplate`**

Do `gui/src/Presentation/Workflow/WorkflowDetailTemplate.php` přidej k `use`

```php
use Donut\Gui\KeyMap;
```

a mezi vlastnosti (za `$rootPath`):

```php
	public ?KeyMap $keys = null;

	/** Jméno klíče z adresy; null = nic není vybrané. */
	public ?string $selectedKey = null;
```

- [ ] **Step 2: Rozšiř `renderDetail`**

V `gui/src/Presentation/Workflow/WorkflowPresenter.php` změň hlavičku metody na

```php
	public function renderDetail(string $name, ?string $key = null): void
```

a doplň `use Donut\Gui\KeyMap;` k ostatním `use`.

Na konci metody, za `$template->workflowProblems = …`, přidej:

```php
		$template->keys = KeyMap::of($workflow);

		// Prázdný řetězec z adresy znamená „nic nevybráno", ne klíč jménem "".
		$template->selectedKey = ($key ?? '') === '' ? null : $key;
```

- [ ] **Step 3: Vypiš klíče a zvýrazni v `steps.latte`**

V `gui/src/Presentation/Workflow/steps.latte` změň hlavičku bloku `steps` na

```latte
{define steps, array $steps, Donut\Gui\StepPath $path, Donut\Gui\ProblemMap $problems, Donut\Gui\KeyMap $keys, ?string $selected}
```

Řádek `<li>` nahraď:

```latte
		<li class="{$keys->classAt($at, $selected)}">
```

Za blok, který vypisuje problémy (za `{/foreach}` na řádku s
`{include problem, …}`), vlož výpis klíčů:

```latte
			{var $reads = $keys->readsAt($at)}
			{var $writes = $keys->writesAt($at)}
			<div class=keys n:if="$reads || $writes">
				<span n:if="$reads">čte
					{foreach $reads as $k}<a n:href="this, key: $k">{$k}</a>{sep}, {/sep}{/foreach}
				</span>
				<span n:if="$writes">zapisuje
					{foreach $writes as $k}<a n:href="this, key: $k">{$k}</a>{sep}, {/sep}{/foreach}
				</span>
			</div>
```

A **všechna tři rekurzivní volání** `{include steps, …}` doplň o nové parametry:

```latte
					{include steps, steps: $step->then, path: $at->child('then'), problems: $problems, keys: $keys, selected: $selected}
```

```latte
					{include steps, steps: $step->else, path: $at->child('else'), problems: $problems, keys: $keys, selected: $selected}
```

```latte
				{include steps, steps: $step->steps, path: $at->child('steps'), problems: $problems, keys: $keys, selected: $selected}
```

- [ ] **Step 4: Předej nové proměnné z `detail.latte`**

V `gui/src/Presentation/Workflow/detail.latte` uprav volání bloku `steps`:

```latte
{include steps, steps: $workflow->steps, path: $rootPath, problems: $problems, keys: $keys, selected: $selectedKey}
```

A nad nadpis `Kroky` přidej řádek, který ukáže, co je vybrané, a nabídne
zrušení výběru:

```latte
<p n:if="$selectedKey !== null">
	Vybraný klíč: <code>{$selectedKey}</code>
	<a n:href="this, key: null">zrušit výběr</a>
</p>
```

- [ ] **Step 5: Doplň styly**

V `gui/src/Presentation/@layout.latte` doplň do `<style>`:

```css
	li.write { background: #eafbea }
	li.read { background: #eef3fb }
	li.write.read { background: #f4f0e6 }
	.keys { font-size: .85em; color: #666 }
	.keys a { color: #06c }
```

- [ ] **Step 6: Ověř naživo**

Run:
```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut/docs/workflows/donut
php -S 127.0.0.1:8200 -t . /home/honza/Dokumenty/Projekty/donut-org/donut/gui/www/index.php >/dev/null 2>&1 &
sleep 2
B='http://127.0.0.1:8200/?presenter=Workflow&action=detail&name=card-dev'

echo -n "bez klíče, HTTP: "; curl -sL -o /tmp/v2-plain.html -w '%{http_code}\n' "$B"
echo -n "zvýrazněných kroků bez klíče (má být 0): "
grep -c 'li class="write\|li class="read' /tmp/v2-plain.html || true

echo -n "s klíčem repo, HTTP: "; curl -sL -o /tmp/v2-repo.html -w '%{http_code}\n' "$B&key=repo"
echo "zvýrazněné kroky pro repo:"
grep -o 'li class="[a-z ]*"' /tmp/v2-repo.html | sort | uniq -c

echo -n "chyb a varování (má být 0): "
grep -c 'class="error"\|class="warning"' /tmp/v2-repo.html || true

kill %1
```

Expected: obojí HTTP 200; bez klíče žádný zvýrazněný krok; s `key=repo`
**jeden `write` a tři `read`** — tolik jich `card-dev` má (`repo-find` ho
zapisuje, čtou ho `workspace-init`, `workspace-finish` a `workspace-pr`).
Nula chyb a varování v obou případech.

Když čísla nesedí, **nepřepisuj očekávání** — vypiš si, co `KeyMap` pro `repo`
vrací, porovnej s `card-dev.json` a nahlas to.

- [ ] **Step 7: Ověř vnořený krok**

`sync.json` má `foreach`. Ověř, že se zvýrazní i krok uvnitř těla:

Run:
```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut/docs/workflows/donut
php -S 127.0.0.1:8201 -t . /home/honza/Dokumenty/Projekty/donut-org/donut/gui/www/index.php >/dev/null 2>&1 &
sleep 2
curl -sL -o /tmp/v2-sync.html 'http://127.0.0.1:8201/?presenter=Workflow&action=detail&name=sync&key=cards'
echo -n "HTTP: "; curl -sL -o /dev/null -w '%{http_code}\n' 'http://127.0.0.1:8201/?presenter=Workflow&action=detail&name=sync&key=cards'
grep -o 'li class="[a-z ]*"' /tmp/v2-sync.html | sort | uniq -c
kill %1
```

Expected: HTTP 200, **pět `write` a pět `read`**, a ani jeden `write read`.
V `sync` se `cards` zapisuje v pěti krocích a čte v pěti jiných, a **všech
deset leží uvnitř těla `foreach`** — takže to zároveň ověřuje, že zvýraznění
prochází do vnořených kroků.

Když čísla nesedí, **nepřepisuj očekávání** — vypiš si, co `KeyMap` pro
`cards` v `sync` vrací, a nahlas to.

- [ ] **Step 8: Spusť obě sady a PHPStan**

Run:
```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut/gui && vendor/bin/tester tests -C && vendor/bin/phpstan analyse
cd /home/honza/Dokumenty/Projekty/donut-org/donut && vendor/bin/tester tests -C && vendor/bin/phpstan analyse
```
Expected: GUI 9 testů, donut 27 testů, PHPStan bez chyb v obojím.
`Latte.TemplatesCompile.phpt` musí projít — hlídá, že se šablony zkompilují.

- [ ] **Step 9: Commit**

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
git add gui/src/Presentation/Workflow gui/src/Presentation/@layout.latte
git commit -m "GUI: klíče jako odkazy, zvýraznění vybraného klíče"
```

---

### Task 5: Readme a zadání

**Files:**
- Modify: `gui/readme.md`
- Modify: `docs/zadani.md`

**Interfaces:**
- Consumes: hotové vrstvy 2.
- Produces: nic.

- [ ] **Step 1: Doplň `gui/readme.md`**

Do sekce, která popisuje, co je v GUI vidět, doplň tok klíčů:

- u každého kroku je vidět, které klíče čte a které zapisuje
- klíč je odkaz; kliknutím se zvýrazní všechny kroky, kde figuruje — zápis
  jinou barvou než čtení
- výběr drží adresa (`&key=repo`), takže **jde poslat odkazem**
- podmíněný zápis se pozná z toho, že zvýrazněný krok leží uvnitř `if` nebo
  `foreach`

V sekci „co zatím není" škrtni tok klíčů a nech tam zápis a builder.

Skutečnou adresu si ověř spuštěním, nepiš ji od oka.

- [ ] **Step 2: Odškrtni vrstvu 2 v zadání**

V `docs/zadani.md`, bod 5, nahraď řádek

```
   - vrstva 2: tok klíčů — viz `superpowers/specs/2026-08-06-gui-vrstva2-design.md`
```

zněním

```
   - ~~vrstva 2: tok klíčů~~ — hotovo,
     viz `superpowers/specs/2026-08-06-gui-vrstva2-design.md`
```

Vrstvu 3 nech neodškrtnutou.

- [ ] **Step 3: Ověř odkazy**

Run:
```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
for p in docs/superpowers/specs/2026-08-06-gui-vrstva2-design.md gui/src/KeyMap.php gui/tests/KeyMap.ValidatorContract.phpt; do
	test -e "$p" && echo "OK  $p" || echo "CHYBÍ  $p"
done
```
Expected: třikrát `OK`.

- [ ] **Step 4: Commit**

```bash
cd /home/honza/Dokumenty/Projekty/donut-org/donut
git add gui/readme.md docs/zadani.md
git commit -m "GUI: readme a odškrtnutí vrstvy 2 v zadání"
```
